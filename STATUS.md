# HayaosiApp 開発ステータス

> 運用ルール:
> - 更新日は各見出しに書く(冒頭に最終更新日を置くと必ず本文とズレるため置かない)
> - **このファイルの上限は150行**(超えるとフックが警告する)。超えたら「最新更新」の古い日付の項目から `STATUS_ARCHIVE.md` へ移す。現在フェーズ・作業中宣言・次のタスク・決定事項メモは移動対象外
> - 過去の経緯を調べるときだけ `STATUS_ARCHIVE.md` を読む

現在フェーズ:**v1.0(通信対戦)クライアント実装・Firebase基本セットアップ済み、実機2台検証待ち**
- Firebaseプロジェクトの基本設定とSimulatorでの疎通確認は完了。暫定セキュリティルールを使用中で、本格ルールは未対応(リリースブロッカー)
- **CPU対戦があるため、Firebase待ちの間もオフラインで対戦画面の開発・検証ができる**(対戦タブ →「ひとりで(CPU対戦)」)
- 同時押し時に進行が停止する現象を1回確認済み。ただし再現条件・再現率を含む実機2台での体系的な検証は未実施
- 実機2台での通信対戦の通し・反復検証、早押し遅延、切断・再入室、ホスト切断は未検証

---

## リリース計画

**8/11公開の計画は延期(2026年8月7日決定)。新しい目標日は未定。** 対戦体験(タップ即回答方式)とホーム画面を固めてから日程を引き直す。当時の日割り計画は `STATUS_ARCHIVE.md` を参照。

### 申請素材の状況

| 項目 | 状態 |
|---|---|
| プライバシーポリシー | **公開済み**:https://saikyo-app-team.github.io/app-privacy/(App Store Connect の Privacy Policy URL に入れる) |
| アプリ名 | **確定:「マナビート」**(サブタイトル「友達と早押し勉強対戦」)。Bundle ID・リポジトリ名・ターゲット名は `HayaosiApp` のまま。**J-PlatPatでの商標確認は未実施(たける・申請前に必ず)** |
| アイコン | **完了**:マナビートの鉛筆型早押しボタン画像を設定済み |
| スクリーンショット | 未着手 |
| App Store URL | 未確定。確定したら `Services/AppLinks.swift` の `appStoreURL` に設定(招待文面に自動で載る) |
| Apple Developer Program | 有効(チーム `LL98RL72H4`) |

---

## 担当分担(2026年8月8日 変更)

| 担当 | 範囲 |
|---|---|
| **たける(Claude含む)** | 大枠UI・対戦体験、英単語の分類・データ管理とボリューム拡充、効果音・ボイス |
| **TOKIYA-YAMAMOTO** | バックエンド全般、追加テスト、既存バグの改善、通信対戦の実機検証 |

---

## 作業中宣言(着手前にここへ記入 → 完了したら消して「最新更新」へ)

| 担当 | 対象ファイル | 内容 | 開始日 |
|---|---|---|---|

---

## 最新更新(2026年8月9日)

- TOKIYA-YAMAMOTO(Codex): **Firestore本格ルールへクライアントを対応**。プロフィールと `friendCodes/{code}` を原子的に作成し、既存ユーザーの索引不足・コード競合を安全に補修
  - フレンド追加を索引の直接参照へ変更し、招待へ `fromUID` を追加。Rulesテストへ既存ユーザー移行とクライアント形式の一連操作を追加
  - 検証:Local EmulatorのRulesテスト11件、Swiftテスト59件、Simulator向けビルドすべて成功。**本番ルール・実在Firebase・実機2台は未変更/未検証**

- Claude(たける側): **回答した瞬間に得点と正誤を反映し、答える人が残っていなければ待たずに発表へ進むようにした**(たけるの指示)
  - **回答のたびに、その時点で確定した得点へ置き直す**。毎回ゼロから計算し直すので、後から順位が入れ替わっても二重加算にならない。誤答の−10も回答した瞬間に反映される
  - オンラインはホストが問題中も `players/*/score` を更新する。現在値と違う項目だけを書くので、自分の書き込みで観測が再帰しない。**RTDBのスキーマは変更していない**(トキヤ氏へ:ホストの書き込み頻度だけが上がる)
  - CPU対戦は、参加を見送ったCPUを待ち対象から外した(以前は不参加CPUが1体でもいると必ず制限時間まで待っていた。CPU7体なら7割以上の問題が該当)
  - 出題中も確定した正誤を○/×で表示する(得点が同時に動くため、正誤だけ伏せても意味がない)。正解音は発表時ではなく回答時に鳴らし、発表では誰も取れなかった時だけ時間切れ音を鳴らす
  - 検証:テスト59件パス(即時加点・即時減点・順位の置き直し・不参加CPUを待たない、の4件を追加/書き換え)。**Simulatorでの通し確認とオンライン実機2台は未検証**

- Claude(たける側): **制限時間の既定を5秒にし、全員が回答し終えたら制限時間を待たずに発表へ進むようにした**(たけるの指示による修正)
  - `QuizDefaults.timeLimit` を5秒、選択肢を5/10/20/30秒に変更(Codex実装時の25秒は取り違え)
  - CPU対戦は `finishQuestion` を時間切れと「全員回答済み」で共有。オンラインもホストが同じ判定で採点し、スナップショットが複数回届いても二重加算しないよう `scoredQuestionIndex` でガード
  - 全員が回答権を使い切ったかは正誤を問わず `game.answers` の顔ぶれで判定する(誤答も回答済みとして数える)
  - 検証:テスト57件パス(「全員回答で制限時間前に発表」1件追加)。SimulatorでCPU対戦4問を完走し、CPU回答時の順位バッジ・×印・並び替え・リザルトまで確認。**オンライン実機2台は未検証**

- Codex(たける側): **対戦を制限時間終了時の一括採点へ変更**。正解者内の回答時刻順で+20/+10/+5/4位以降+1、不正解−10、無回答0とした
  - 正解者が出ても制限時間までは全員の回答を受け付け、CPU・オンライン共通の純粋ロジックで採点。RTDBの `reveal.scorerID/byTimeout` は正解順位順の `correctIDs` へ変更し、複数正解者の○印と発表表示に対応
  - 制限時間の初期値は25秒、選択肢は10/20/25/30秒。検証:ビルド成功、テスト56件パス。SimulatorのCPU対戦通し操作とオンライン実機2台は未検証

- Codex(たける側): **対戦を文字送り型へ一本化し、丸アイコン列のスコアボードへ刷新**。
  - 出題形式の選択UI・旧形式専用の進行処理・RTDB設定を削除。回答順はCPU・オンライン共通で `game.answers` のサーバー時刻だけから決め、誤答者は `game.failed` へ記録する
  - スコアボードはホスト→入室順を基本に、回答した順で左へ並び替える。丸内は人型アイコン、左上に順位、右下に赤い○/×を表示。順位バッジはライト/ダーク両方に追従し、8人時は横スクロールで崩れない
  - CPU対戦はCPUを最大7体(自分を含め8人)まで選択可能にした。検証:ビルド成功、テスト50件パス。Simulator通し操作はこの環境の補助アクセス権不足で未検証

- Claude(たける側): **Simulatorスクリーンショットのトークン節約方針をCLAUDE.mdへ追記**(たける発案)。座標探しはread_page/find等を使い撮らない、見た目確認は最終状態のみ上限2枚、ロジックのみの変更は撮らない、重い通し確認はたけるが自分のSimulatorで見る、の4点を「完了の定義」に明文化

- TOKIYA-YAMAMOTO(Codex): **Firebase本格セキュリティルールの目標仕様とLocal Emulator自動テストを追加**。
  - Realtime Database:待機中の部屋だけ参加前の確認を許可し、対戦開始後は参加者のみ読み取り可。ルーム作成はホスト本人、ゲーム進行・得点更新はホストのみ、参加者は自分の参加・退出だけを許可
  - Firestore:`users` と `friendCodes` の一覧取得を禁止。プロフィールとフレンドコード逆引きを同時作成し、招待は送信者が登録済みフレンドへ送る場合だけ許可
  - `firebase.json` / `database.rules.json` / `firestore.rules` / `FirebaseRulesTests/rules.test.js` / Node.js開発専用依存を追加
  - `demo-hayaosiapp` のEmulatorでルールテスト**9件パス**。実在するFirebaseプロジェクトや本番データには接続していない
  - **本番ルールは未変更**。`AuthService` / `FriendService` が `friendCodes/{code}` と招待の `fromUID` に対応するまで公開しない
  - npm監査は中程度6件(すべてローカル開発用Firebase CLIの間接依存)。iOSアプリには同梱されない。自動修正・強制更新は未実施

- 人間: **同時押し時に対戦進行が停止する現象を1回確認**。条件の詳細と再現率は未整理。
  - コード上の有力仮説:`OnlineBattleSession+Host` が1件目を採点中、2件目の状態更新を `isEvaluatingAnswer` で見送った後、書き込み完了時に再判定を呼ばないため、未処理回答が残る競合状態
  - 修正前の実機2台検証:文字送り型で「正解+正解」「誤答+正解」「誤答+誤答」を各10回。同じ端末の連打も確認し、両端末の画面録画・ホスト側Xcodeログ・問題番号・選択肢・停止した画面を記録する
  - 修正後は同じ表を再実行し、2件目が必ず処理されること、得点の二重加算がないこと、先着順がサーバー時刻順であること、時間切れまで待たず進行することを合格条件とする

これより古い更新は `STATUS_ARCHIVE.md` にある。

---

## 次のタスク(優先順)

### 担当:たける側(Claude含む)— コンテンツ・大枠UI・対戦体験

1. **対戦画面UIの強化(次に着手)**:対戦中はタブバーを隠し、対戦画面の演出・情報配置を改善する。効果音・ボイスも追加・差し替えする
2. **プロフィール機能の強化**:マイページのプロフィール表示・編集を充実させる。範囲(アイコン/自己紹介/戦績表示など)は着手前に決める。オンライン同期を伴う項目はトキヤ氏と調整
3. **フレンド機能の強化**:フレンド一覧・招待まわりの使い勝手を上げる。Firestoreのデータ構造に触る変更はトキヤ氏と調整
4. **広告の表示**:表示場所と頻度を先に決める。SDK追加で `project.yml` を触るため事前宣言が必要。プライバシーポリシーとApp Storeのデータ収集申告の更新も伴う(要件定義書の「v1.0は課金なし」方針との整合も確認する)
5. **英単語のボリューム拡充**:`junior_high.json` / `high_school.json` へ、たける・トキヤが直接入力する。現200語はテストデータなので順次刷新し、各カテゴリのdifficulty 1〜5を充実させる
6. **対戦結果の保存 →「直近の対戦」表示**:対戦タブに順位・正答数を出す
7. **習熟度の可視化(v1.1)**:`visibleCount`を `AnswerRecord` へ保存し、学習タブに苦手順の一覧を出す
8. 実機で音・振動を確認する
9. **英単語JSON更新の自動検知(保留)**:開発第一段階は `QuestionSeeder.dataVersion` を手動で増やす現方式を継続する。単語更新が本格化して運用負担になった段階で、JSONのハッシュ比較による自動再投入を検討する

### 担当:TOKIYA-YAMAMOTO — テスト・バグ改善・オンライン本番対応

1. **実機2台での通信対戦検証(最優先)**:ルーム参加からリザルトまで通しで動かし、遅延・同時押し・切断・再入室・ホスト切断を確認する
2. **セキュリティルールの本格設計(リリースブロッカー)**:`FIREBASE_SETUP.md` §4-1を参照。アプリ側変更を伴う場合は着手前に調整する
3. **対戦の追加テストと既存バグの改善**:`CPUBattleSession`を含む進行ロジックの不足ケースを追加し、発見した不具合を修正する
4. **ジャンルPickerの不具合改善**:`.constant`で反応しない状態を直す。ジャンルが1つの間は非表示または `disabled` にする
5. 通信対戦検証後の切断・再入室・遅延対策
6. Cloud Functions開発(v1.5準備):採点のサーバー移行。着手前に作業中宣言必須

### 担当:未定

1. ルーム自動削除(使用済みルームがRTDBに残る) / 入力式回答(v1.5)

---

## 決定事項メモ(実装詳細の正はここ)

- プロジェクト管理はXcodeGen(pbxprojの手編集・コンフリクトを避けるため。2人開発と相性が良い)
- 誤答選択肢はJSONに持たせず、同品詞の他単語の意味からシード時に決定的に自動生成(データ作成コスト削減)
- 復習リスト:不正解で登録・回数加算、正解すると自動で外れる(「克服したら卒業」方式)
- ニックネームは `@AppStorage("nickname")` が正。サインイン済みなら `users/{uid}` に自動同期
- 問題データ更新時は `QuestionSeeder.dataVersion` を上げると次回起動時に再投入される
- 英単語は**カテゴリと難易度を分離**する。現在のカテゴリは `junior_high` / `high_school` の2つだけで、各カテゴリに `difficulty: 1...5` がある。大学英語は作らず、TOEIC・TOEFL・英検・IELTSは将来必要になった時に別カテゴリとして追加する
- 正データは `Resources/junior_high.json` / `Resources/high_school.json`。IDは `jh_####` / `hs_####`。同じファイル内の単語重複は禁止し、中学・高校をまたぐ重複は許可する。アプリ内入力ツールは持たずJSONを直接編集する
- カテゴリ・難易度選択は一人練習・CPU対戦・オンライン対戦に対応。オンラインはRTDBの `settings.wordCategory` / `settings.wordDifficulty` で共有し、旧ルームで両方が無い場合だけ全問題を対象にする
- 対戦中はタブバーを隠し、問題と対戦状況へ集中できる画面にする。試合開始前はCPU・オンライン共通の開始合図を1問目の前だけ表示し、時間は `BattleRules` で管理する
- バックエンド全般(Firebase/Auth/DB/セキュリティルール/Cloud Functions等)は原則としてTOKIYA-YAMAMOTO氏が担当する。たける側の機能で変更が必要な場合は、独断で実装せず担当間で調整する
- 通信対戦の進行権威はホスト端末(v1.0)。Cloud Functions採点はv1.5で検討
- 対戦の出題はホストが端末内の問題からシャッフルしてRTDBに配信(全員同じ選択肢順)
- フレンドは片方向フォロー方式(相互承認なし)。招待は相手の `invites` サブコレクションに書き込む
- 対戦ルール定数は `Services/Battle/BattleRules.swift`、文字送りの間隔は `ProgressiveReveal.characterInterval` が唯一の出典(ドキュメントに数値を書かない)
- **対戦は文字送り型のみ**:問題文を1文字ずつ表示し、4択は全員に最初から見せる。選択肢タップが回答となる。正解者内の回答時刻順で+20/+10/+5/4位以降+1、不正解−10、無回答0
- **得点は回答のたびに、その時点で確定したぶんを反映する**(発表まで待たせない)。毎回ゼロから計算し直して問題開始時の得点へ置き直すので二重加算しない。発表へ進むのは「制限時間切れ」か「回答しうる全員が答え終えた」時。CPU対戦では参加を見送ったCPUを待ち対象に含めない
- 文字送りの表示量は経過時間だけから計算する(`ProgressiveReveal`)。端末間で表示を同期する通信は持たない。全文表示になるのは発表時
- 回答時に「選択肢・時刻・表示文字数(`visibleCount`)」を記録する。学習履歴への保存はタスク6で対応予定
- 問題データの `definition` は現在の出題では未使用(将来のジャンル拡張・入力式用に保持)
- 署名(`DEVELOPMENT_TEAM`)は `project.yml` に書かず `Config/local.xcconfig`(git管理外)で各自が指定する
- テストはロジックのみ(`Tests/`)。通信層はユニットテスト対象外にし、実機の通し確認で担保する
- 文書の役割:要件定義書=スコープと意図の正 / 本節=実装詳細の正 / `STATUS_ARCHIVE.md`=完了した過去記録
