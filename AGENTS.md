# AGENTS.md(Claude以外のAIエージェント向け)

コーディング規約・ディレクトリ構成・XcodeGen運用・ビルド方法は **`CLAUDE.md`(このディレクトリ内)を参照**。
二重管理による乖離を防ぐため、このファイルには規約を書かない。

追加で読むもの(順番):
1. `STATUS.md` — 最新の進捗・作業中宣言・次のタスク・決定事項メモ
2. **`docs/CODING.md` — Swiftを書く前に必ず読む**(規約・命名・テスト方針)
3. `要件定義書_勉強系早押し対戦アプリ.md` — 何を作るか・なぜそう決めたか(大枠)
4. `README.md` — clone後のセットアップ(XcodeGen・署名)/ `FIREBASE_SETUP.md` — Firebase側の手順とデータ構造

**文書の優先順位(重要):** 要件定義書は**スコープと意図**の正だが、実装詳細(定数値・データ構造・画面の細部)は追いかけていない。
実装詳細は `STATUS.md` の「決定事項メモ」とコードが正。食い違いを見つけたら勝手に実装を要件へ寄せず、STATUS.md に記録して確認を取る。

絶対ルール(2人開発):
- 着手前に `STATUS.md` の「作業中宣言」に記入する。**依頼プロンプトに書かれていなくても必須**(スキップした作業は受け入れ時に指摘される)。相手に見えるのはpushした時点からなので、長い作業や `Models/`・`project.yml` を触る作業では宣言だけを先にコミット&pushする(コミットしないまま作業が続く場合は宣言を残しておく)。他の開発者が宣言中のファイルは編集しない
- 変更したら `STATUS.md` に記録する
- 大きな変更(SwiftDataモデル・`project.yml`・リネーム)は事前に編集対象ファイルを明示して確認を取る
- `.xcodeproj` は手編集禁止。ファイル追加・削除後は `xcodegen generate` を実行する
