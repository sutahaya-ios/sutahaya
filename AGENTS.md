# AGENTS.md(Claude以外のAIエージェント向け)

**ルールは `CLAUDE.md`(このディレクトリ内)がすべて。** 二重管理で文言がずれるため、このファイルにはルールを書かない。
リポジトリを初めて扱うとき、または `CLAUDE.md` が更新されたときだけ確認し、通常タスクごとに運用文書一式を読み直さないこと。

通常タスクは次の5ステップだけを標準とする。

1. ユーザーの指示と `CLAUDE.md` の常時禁止事項を確認
2. 指定された対象ファイルを直接確認
3. 編集時のみ、必要に応じて `STATUS.md` の該当部分と `git status --short` を確認
4. 最小差分で実装
5. 変更対象だけdiff確認し、リスクに応じた最小検証を1回実施

`STATUS.md`、`docs/MODULE_MAP.md`、`docs/CODING.md`、README、要件・設計文書は `CLAUDE.md` の条件に該当するときだけ読む。
