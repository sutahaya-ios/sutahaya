# 要件定義書:勉強系早押し対戦アプリ(仮称)

- 作成日:2026年7月11日
- 最終更新:2026年8月3日
- バージョン:1.0(ドラフト)
- 作成者:たける

> **この文書の位置づけ**
> ここに書くのは**大枠(何を作るか・なぜそう決めたか・スコープの内外)**まで。
> 実装レベルの詳細(定数値・データ構造・画面の細部)は追いかけず、その正は `STATUS.md` の「決定事項メモ」とコードに置く。
> 食い違いを見つけた場合、**スコープと意図はこの文書が正・実装詳細は `STATUS.md` が正**として扱う。

---

## 1. プロジェクト概要

### 1.1 コンセプト

**「勉強版みんはや」— 学生が英単語・資格・SPIなどの勉強を、友達との早押し対戦で楽しく継続できるiOSアプリ。**

### 1.2 背景・課題

- 資格試験・受験・SPIなどの勉強は一人では継続しにくく、モチベーション維持が難しい。
- 「みんなで早押しクイズ(みんはや)」は対戦の楽しさで高い継続性を実現しているが、コンテンツは雑学中心であり、学習目的には使えない。
- 「対戦の楽しさ」と「学習効果」を両立するアプリには明確な空白がある。

### 1.3 解決アプローチ

早押しクイズの対戦体験(緊張感・競争・友達との盛り上がり)を、学習コンテンツ(英単語・資格・SPI等)に適用する。勉強を「一人で耐えるもの」から「友達と遊ぶもの」に変える。

---

## 2. ターゲットユーザー

| 項目 | 内容 |
|---|---|
| 主ターゲット | 学生(高校生・大学生) |
| 利用シーン | 試験前に友達と同じ部屋で対戦/離れた友達とオンライン対戦/隙間時間の一人練習 |
| 想定ニーズ | 英単語テスト対策、資格試験勉強、SPI・就活筆記対策 |

---

## 3. 競合と差別化

### 3.1 競合

| アプリ | 特徴 | 本アプリとの違い |
|---|---|---|
| みんなで早押しクイズ | リアルタイム早押し対戦の完成形 | コンテンツが雑学中心で学習用途に不向き |
| 英単語アプリ(mikan等) | 学習効率特化 | 一人用でソーシャル性・対戦性がない |
| Kahoot! | 教室向けクイズ大会 | 教師主導の設計で、友達同士の気軽な対戦に不向き |

### 3.2 差別化ポイント

- **早押し対戦 × 学習コンテンツ**の組み合わせ(勉強系のみんはや)
- 同じ部屋でも遠隔でも、ルームコード1つで友達と対戦できる手軽さ
- 対戦履歴が学習履歴として蓄積され、復習につながる

---

## 4. リリース計画(スコープ定義)

| バージョン | 内容 | 目的 |
|---|---|---|
| **v0.5** | 一人練習モード+英単語コンテンツ(通信なし・SwiftDataのみ) | 出題エンジンとUIの検証 |
| **v1.0** | ルームコード方式のオンライン対戦(同室・遠隔共通) | コア価値「友達と対戦」の実現 |
| **v1.5以降** | ランダムマッチ、レート/ランキング、ジャンル拡張(資格・SPI風)、ユーザー投稿問題 | 拡張 |

**v1.0で作らないもの(明示的スコープ外):** ランダムマッチ、レート機能、キャラクター育成要素、ユーザー投稿問題、Android版。

### 4.1 v1.0リリースまでのマイルストーン(2026年8月4日〜11日)

**目標:2026年8月11日 App Store 公開**(申請は8月8日。審査に2〜3日を見込む)

| 期日 | 到達点 | 担当 |
|---|---|---|
| 8/4 | Firebase稼働(匿名Auth・RTDB・Firestore)/ アプリ名確定 | TOKIYA-YAMAMOTO / たける |
| 8/5 | 実機2台で早押しの体感遅延を確認(技術検証スパイク完了) | たける |
| 8/6 | セキュリティルール本番化(§4-1相当の強化完了) | TOKIYA-YAMAMOTO + Claude |
| 8/7 | 問題データ200語・アイコン・プライバシーポリシー・審査用素材が揃う | 全員 |
| 8/8 | 実機で通し確認 → **App Store 申請** | たける |
| 8/11 | **公開** | たける |

日単位の作業割りと、v1.0.1へ送る項目は `STATUS.md` の「リリース工程」を参照。

---

## 5. 機能要件

### 5.1 対戦モード(v1.0のコア機能)

#### 5.1.1 ルーム機能

- ホストがルームを作成すると**参加コード(4〜6桁)**が発行される。
- 参加者はコードを入力して入室する(同じ部屋の友達も遠隔の友達も同一方式)。
- ルーム内で対戦設定(ジャンル・問題数・制限時間)を確認でき、ホストが開始する。
- 想定同時対戦人数:2〜8人(上限は技術検証後に確定)。

#### 5.1.2 早押し判定仕様

- 問題文表示後、任意のタイミングで早押しボタンを押せる。
- **サーバー到達順**で最初に押したプレイヤーに回答権が与えられる(通信遅延による差は仕様として許容)。
- 回答権を持つプレイヤーが回答している間、他プレイヤーの操作はロックされる。
- **選択肢は回答権を得た本人にのみ表示する**(押す前は問題文だけが見える=ブラインド早押し)。全員に選択肢を見せると「読んだ瞬間に押す」だけのゲームになるため。
- **正解:+1ポイント/不正解:−1ポイント**。
- 不正解の場合、回答権は**次に早く押していたプレイヤー**に移行する(押下順キューを保持)。誤答したプレイヤーはその問題に再度押せない(お手つきロック)。
- 押下者が全員誤答した場合は、その問題の制限時間を仕切り直して早押し受付に戻す。
- 誰も押さないまま問題の制限時間が経過した場合、その問題は流れて次の問題へ。

進行の時間制御(数値の正は `BattleRules`):

| 項目 | 値 | 備考 |
|---|---|---|
| 回答権を得てから回答するまでの制限時間 | 10秒 | 超過は不正解扱い(−1点)で次の押下者へ移行 |
| 正解・時間切れの発表時間 | 3秒 | 経過後に次の問題へ |
| 参加人数 | 2〜8人 | 2人未満では開始できない |

##### 出題形式(早押しの駆け引き)

**単語バトルの標準は「文字送り型」**とし、設定で「速答型」にも切り替えられる。どちらが受け入れられるかはリリース後の反応を見て判断するため、片方に決め打ちしない。

| 形式 | 出題 | 駆け引き | 位置づけ |
|---|---|---|---|
| **文字送り型** | 意味・説明文を**少しずつ表示** → 単語を4択 | 早く押すほど手がかりが少ない。押すか待つかの判断が勝負を分ける | **標準** |
| 速答型 | 単語を表示 → 意味を4択 | 押すタイミングの駆け引きはない。知識と反射の勝負 | 選択可 |

- ホストが選んだ形式が全員に適用される(試合中の変更はできない)。一人練習でも同じ形式を選べる
- 文字送りは**誰も押していない間だけ進む**。誰かが押した後は全文を表示する(回答者が読めないと答えられず、誤答で回答権が移った人も読み直す必要があるため)
- 表示の進み具合は問題の開始時刻からの経過時間だけで決まるので、全端末で同じ見え方になる。全文が出そろうまでの時間は制限時間に比例させる(具体値はコードが正)

#### 5.1.3 試合ルール設定(ホストが変更可能)

| 設定項目 | 内容 | デフォルト |
|---|---|---|
| 問題数 | 選択式(5 / 10 / 15 / 20問) | 10問 |
| 1問あたり制限時間 | 選択式(10 / 20 / 30秒) | 20秒 |
| ジャンル | 英単語(v1.0)/以降拡張 | 英単語 |

自由入力ではなく選択式にしたのは、極端な設定(1問・300秒など)で対戦が成立しなくなるのを防ぐため。

- 最終順位はポイント順。**同点は同順位**とする。

### 5.2 回答形式(出題エンジン)

複数の回答形式をサポートする拡張可能な設計とする。
※「回答形式」=答え方(4択/入力式)。「出題形式」=問題文の見せ方(速答型/文字送り型、§5.1.2)。別の軸なので混同しないこと。

| 形式 | 内容 | 導入時期 |
|---|---|---|
| 4択 | 選択肢から1つ選ぶ | v0.5 |
| 入力式 | テキスト入力で回答(表記ゆれ・大文字小文字の正規化が必要) | **v1.5へ延期**(v1.0はスキーマ上の受け皿のみ) |
| その他(並べ替え等) | 将来拡張 | v1.5以降 |

- 問題データは形式(type)フィールドを持つ共通スキーマで管理し、形式の追加が既存データに影響しない構造とする。

### 5.3 一人練習モード(v0.5)

- 通信なしで、端末内の問題データから出題。
- 出題エンジン(`QuizSession`)を対戦と共通化する。画面は練習用に別途持つ(早押しの競り合いがないため)。出題形式・回答形式は対戦と同じものを選べる。
- 間違えた問題は自動で**復習リスト**に登録され、復習モードで再出題できる。

### 5.4 学習履歴・結果表示

- 試合終了後にリザルト画面(順位、正答数、獲得ポイント)を表示。
- 対戦・練習の正誤履歴をSwiftDataに保存し、間違えた問題を復習リストに反映する。

### 5.5 コンテンツ(v1.0時点)

- **英単語**(自作データ)からスタート。単語と意味の対応データは著作権リスクが低く、作成コストも低い。
- 1語につき「単語・品詞・意味・説明文」を持つ。**説明文は文字送り型の問題文**になるため、短すぎると駆け引きが成立しない(20〜30字程度を目安に自作する)。
- 資格・SPI系は**オリジナル問題(SPI「風」問題等)**として順次追加。市販問題集・過去問の転載は行わない(著作権対応、§7.3参照)。

### 5.6 フレンド機能(v1.0スコープ内)

- 毎回コードを教え合わずに友達を呼べるようにするための機能。**v1.0の必須機能とする**。
- ユーザーごとに固定のフレンドコードを発行し、コード入力で相手をフレンドに追加できる(相互承認は求めない片方向方式)。
- ロビーからフレンドへルーム招待を送れ、受け取った側はワンタップで入室できる。

### 5.7 ボット対戦(v1.0スコープ内)

- 通信なしで対戦フローを一通り体験できるモード。目的は2つ:**①友達がいない隙間時間でも対戦形式で練習できる ②Firebase未設定・オフラインでも対戦画面を開発・検証できる**。
- 対戦ルールはオンライン対戦と同一とし、結果は一人練習として学習履歴・復習リストに反映する。

---

## 6. 非機能要件

| 項目 | 要件 |
|---|---|
| 対応OS | iOS 17以降(SwiftData要件に準拠) |
| 対応端末 | iPhone(iPadは当面スコープ外) |
| リアルタイム性 | 早押し判定の体感遅延が対戦の成立を損なわないこと(目標:押下→判定反映まで概ね500ms以内、技術検証で確定) |
| 切断耐性 | 対戦中の一時切断時に再入室できること(v1.0では最低限、途中離脱者を除いて試合続行できればよい) |
| オフライン動作 | 一人練習モード・ボット対戦は完全オフラインで動作 |
| 音・触覚 | 効果音とハプティクスで早押しの手応えを出す。教室・電車での利用を想定し、マナーモード時は鳴らさず、設定で個別にOFFにできること |
| プライバシー | アカウントは匿名(ニックネームのみ)から開始。個人情報の収集は最小限とする |
| コスト | Firebase無料枠(Sparkプラン)内で開発・小規模運用が完結すること |

---

## 7. 技術構成

### 7.1 アーキテクチャ概要

```
[iPhone (SwiftUI)]
   ├─ SwiftData …… 学習履歴・復習リスト・ローカル問題データ(一人練習用)
   └─ Firebase SDK
         ├─ Realtime Database …… ルーム状態同期・早押し判定(トランザクション)・出題データの配信
         ├─ Firestore …… ユーザープロフィール・フレンド・ルーム招待
         ├─ Cloud Functions …… 採点・進行制御などサーバー権威処理(v1.5で検討)
         └─ Firebase Auth(匿名認証)
```

問題データはサーバーに問題マスタを置かず、**端末内のデータからホストが選んでルームへ配信する**方式にした(v1.0)。オフラインの一人練習と同じデータを使えて、Firestoreの読み取り回数も増えないため。ジャンル追加でデータ量が増えたら問題マスタ配信へ移行を検討する。

### 7.2 技術選定と理由

| 領域 | 選定 | 理由 |
|---|---|---|
| UI | SwiftUI | 標準・開発効率・既存経験(Buffito) |
| ローカルDB | SwiftData | iOS標準、学習履歴・復習リストの管理に適する |
| バックエンド | **Firebase** | Realtime DBのトランザクションで「最初に押した1人に回答権を付与」を原子的に実装可能。情報量・無料枠・将来のランダムマッチ拡張性で優位 |
| 認証 | Firebase匿名認証 | 登録の手間なく開始でき、将来アカウント連携に昇格可能 |

**不採用案:** Supabase(早押しの原子的判定を自作する必要があり、Swift SDK・情報量で劣る)、Game Center(P2P型で判定の権威が持てず、チート・切断に弱い。ルームコードUIの自由度も低い)。

### 7.3 リスクと対応

| リスク | 内容 | 対応 |
|---|---|---|
| 著作権 | SPI・資格の市販問題/過去問の転載は侵害リスク | オリジナル問題のみ使用。英単語から開始。ユーザー投稿導入時は監視体制を設計 |
| 早押しの公平性 | 通信遅延により押下順に有利不利が生じる | サーバー到達順判定を仕様として明記。極端な遅延ユーザーへの表示上の配慮は将来検討 |
| リアルタイム実装難度 | 早押し判定・進行同期が本アプリの技術的最難関 | v1.0着手前に**技術検証(スパイク)**として2台実機での早押しプロトタイプを最優先で作る |
| Firebaseコスト | ユーザー増加時の従量課金 | 無料枠監視。Realtime DBの書き込み頻度を設計段階で抑制 |

---

## 8. データモデル(概要)

### 8.1 SwiftData(端末側)

- **Question**(ローカル問題):id、ジャンル、出題形式(速答型/文字送り型)、回答形式(4択/入力)、問題文、選択肢、正答
  - 同じ単語から出題形式ごとに別の問題を作る(問題文と選択肢が入れ替わるため)。復習リスト・履歴も形式ごとに独立して積まれる
- **AnswerRecord**(解答履歴):問題id、正誤、日時、モード(練習/対戦)
- **ReviewItem**(復習リスト):問題id、間違い回数、最終出題日時

### 8.2 Firebase(サーバー側)

- **rooms/{roomId}**(Realtime Database):参加コード、ホストid、参加者一覧、設定(問題数・制限時間・ジャンル)、状態(待機/出題中/リザルト)、その試合の出題データ
- **rooms/{roomId}/buzz**(Realtime Database):現在の問題に対する押下キュー(サーバータイムスタンプ順)
- **users/{uid}**(Firestore):ニックネーム、フレンドコード。配下にフレンド一覧とルーム招待

※実際のフィールド構成は `FIREBASE_SETUP.md` の「データ構造(実装済み)」が正。

---

## 9. 画面一覧(v1.0)

タブ構成(ホーム/ルーム/フレンド/復習/設定)を土台に、以下の画面を持つ。

1. ホーム(一人練習の入口+成績サマリ)
2. 一人練習:設定(ジャンル・出題形式・問題数・制限時間) → 出題 → リザルト
3. 復習リスト(間違えた問題の一覧・復習出題)
4. ルーム:ルーム作成(設定+コード表示)/ルーム参加(コード入力)/ボット対戦設定/フレンドからの招待
5. 待機ロビー(参加者一覧・設定確認・フレンド招待)
6. 対戦画面(問題表示・早押しボタン・回答UI・スコア表示)
7. リザルト画面(順位・正答数・再戦ボタン)
8. フレンド(自分のプロフィールとコード・フレンド一覧・コード入力で追加)
9. 設定(ニックネーム・サウンド等)

---

## 10. 未決定事項(今後の検討)

| # | 項目 | 状態・決定時期の目安 |
|---|---|---|
| 1 | アプリ名 | **決定済み(2026年8月4日):「マナビート」**(サブタイトル「友達と早押し勉強対戦」)。**Bundle ID `com.n.HayaosiApp`・リポジトリ名・ターゲット名は変更しない**(App Store上で別アプリ扱いになるため。表示名のみ差し替える)。アイコンは8/6までに用意 |
| 2 | 入力式回答の正誤判定ルール(表記ゆれ許容範囲) | v1.5(入力式の導入自体をv1.5へ延期。§5.2) |
| 3 | 同時対戦人数の上限 | **暫定確定:8人**。実機検証で見直す |
| 4 | 押下→判定反映のレイテンシ目標値 | **暫定確定:500ms**(§6)。実機2台の検証で確定する |
| 5 | 収益化方針(広告/買い切り/サブスク) | v1.0リリース後 |
| 6 | ランダムマッチのマッチング方式・レート設計 | v1.5企画時 |
| 7 | 早押しの駆け引き方式 | **決定済み(2026年8月3日):標準は文字送り型・設定で速答型に切替可(§5.1.2)。どちらを主にするかはリリース後の反応で見直す** |
| 8 | 配点を早押しの速さで変える案(§5.1.2 案C)の採否 | v1.5以降。±1点ルールの変更がリザルト・履歴に波及するため、まず文字送り型の反応を見てから |

---

## 11. 直近のアクション

最新の進捗と担当は `STATUS.md` を参照(この節は方針レベルの残作業のみ)。

1. **技術検証スパイク:** 実機2台で「押下→先着1名に回答権付与」の判定精度と体感遅延を確認する(Firebaseセットアップ完了後)
2. セキュリティルールの本格設計(リリースブロッカー。`FIREBASE_SETUP.md` §4-1参照)
3. 文字送り型の体感調整(表示ペース・制限時間の初期値)を実機で確認する
4. 英単語データの品質見直し・拡充(現状100語)
