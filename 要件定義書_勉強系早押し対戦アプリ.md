# 要件定義書:勉強系早押し対戦アプリ(仮称)

- 作成日:2026年7月11日
- 最終更新:2026年8月8日
- バージョン:1.0(ドラフト)
- 作成者:たける

> **この文書の位置づけ**
> ここに書くのは**大枠(何を作るか・なぜそう決めたか・スコープの内外)**まで。
> 実装レベルの詳細(定数値・データ構造・画面の細部)は追いかけず、その正は `STATUS.md` の「決定事項メモ」とコードに置く。
> 食い違いを見つけた場合、**スコープと意図はこの文書が正・実装詳細は `STATUS.md` が正**として扱う。

---

## 1. プロジェクト概要

### 1.1 コンセプト

**「勉強版みんはや」— 学生が英単語・資格・SPIなどの勉強を、友達との早押し対戦で楽しく継続できるiOSアプリ。**

### 1.2 背景・課題

- 資格試験・受験・SPIなどの勉強は一人では継続しにくく、モチベーション維持が難しい。
- 「みんなで早押しクイズ(みんはや)」は対戦の楽しさで高い継続性を実現しているが、コンテンツは雑学中心であり、学習目的には使えない。
- 「対戦の楽しさ」と「学習効果」を両立するアプリには明確な空白がある。

### 1.3 解決アプローチ

早押しクイズの対戦体験(緊張感・競争・友達との盛り上がり)を、学習コンテンツ(英単語・資格・SPI等)に適用する。勉強を「一人で耐えるもの」から「友達と遊ぶもの」に変える。

---

## 2. ターゲットユーザー

| 項目 | 内容 |
|---|---|
| 主ターゲット | 学生(高校生・大学生) |
| 利用シーン | 試験前に友達と同じ部屋で対戦/離れた友達とオンライン対戦/隙間時間の一人練習 |
| 想定ニーズ | 英単語テスト対策、資格試験勉強、SPI・就活筆記対策 |

---

## 3. 競合と差別化

### 3.1 競合

(2026年8月8日 App Storeで機能を確認)

| アプリ | 特徴 | 本アプリとの違い |
|---|---|---|
| みんなで早押しクイズ(Q ONE・★4.55/52万件) | ランダムマッチ(レート戦)とフリーマッチ。**ユーザーが作った問題を全国に公開できる**。キャラクターとの「キズナ」でボイス・衣装・スタンプが解放される | コンテンツが雑学中心で学習用途に不向き。**復習・学習履歴が無い** |
| 常識王(4択早押し) | **4択・回答権10秒・誤答で権利喪失**と本アプリに近い設計。1台で最大4人対戦、クイックマッチ、レート | 雑学中心。学習の積み上げが無い |
| 英単語アプリ(mikan等) | 学習効率特化 | 一人用でソーシャル性・対戦性がない |
| Kahoot! | 教室向けクイズ大会 | 教師主導の設計で、友達同士の気軽な対戦に不向き |

**比較して分かった構造的な差(2026年8月8日)**

| 観点 | 早押しアプリ各社 | 本アプリの現状 |
|---|---|---|
| 対戦の入口 | ランダムマッチでワンタップ開始 | **ルームコードが必須**で、友達がいないと始まらない |
| 戻ってくる理由 | レート・ランキング・キャラ育成 | 成績サマリのみで薄い |
| 問題の供給 | ユーザー作問で無限に増える | 収録データのみで天井がある |
| 対戦中のやりとり | スタンプ | なし |
| **学習の積み上げ** | **無い** | **復習リスト・学習履歴・一人練習(本アプリの独自資産)** |

### 3.2 差別化ポイント

- **早押し対戦 × 学習コンテンツ**の組み合わせ(勉強系のみんはや)
- 同じ部屋でも遠隔でも、ルームコード1つで友達と対戦できる手軽さ
- 対戦履歴が学習履歴として蓄積され、復習につながる
- **「何文字目で分かったか」を記録できる**(§5.1.2)。単語ごとの習熟度という、雑学クイズでは原理的に作れない指標を持てる

**エンタメ機能を足すときの原則:** 「対戦するほど学習が進む」構造を崩さない。復習・学習履歴は競合が持っていない唯一の資産であり、ここを薄めると存在理由がなくなる。

---

## 4. リリース計画(スコープ定義)

| バージョン | 内容 | 目的 |
|---|---|---|
| **v0.5** | 一人練習モード+英単語コンテンツ(通信なし・SwiftDataのみ) | 出題エンジンとUIの検証 |
| **v1.0** | ルームコード方式のオンライン対戦+CPU対戦+画面構成の再編(タブ3本) | コア価値「友達と対戦」の実現 |
| **v1.1** | 習熟度の可視化・プロフィール・スタンプ | 継続して戻ってくる理由を作る |
| **v1.5以降** | 目的別カテゴリ(TOEIC/TOEFL/英検/IELTS等)、作問、ランダムマッチ、レート/ランキング、ジャンル拡張 | 拡張 |

**v1.0で作らないもの(明示的スコープ外):** ランダムマッチ、レート機能、キャラクター育成要素、Android版、課金。

**作らないと決めたもの(将来も含む):**

| 機能 | 理由 |
|---|---|
| **テキストチャット** | 未成年が使うため、App Reviewガイドライン1.2により通報・ブロック・モデレーション体制の実装が必須になる。2人体制で運用できない。**スタンプ(定型リアクション)で代替する** |
| **SNS的なタイムライン・投稿** | 同上。フレンドの学習量を受動的に見せる範囲に留める(投稿機能は作らない) |
| **1台を回して複数人で遊ぶ方式(パスアンドプレイ)** | 1ダウンロードで複数人が遊べてしまい、ユーザー数が伸びない。**全員がインストールする形を維持する**(2026年8月8日決定) |

### 4.1 リリース日程

**当初の8/11公開計画は延期(2026年8月7日決定)。新しい目標日は未定。**
回答方式の刷新(選択肢のタップ自体が早押し+回答)と画面構成の再編を優先し、対戦体験が固まってから日程を引き直す。進捗と日程は `STATUS.md` を参照。

### 4.2 課金方針(v1.0では実装しない)

将来入れるなら**問題パックの買い切り**とする(英検2級パック、TOEIC600パックなど)。学習価値と直結し、対戦体験を壊さないため。

- サブスクは学生ターゲットには重い
- 広告は早押しの緊張感を壊すため入れない(特に対戦後の全画面広告)
- キャラクター・スタンプ課金はみんはやの土俵であり、制作コストに見合わない

**v1.0は全機能を無料とし、問題データを「パック」単位で構造化しておく**ことで、後から一部を有料化できる状態にしておく。

---

## 5. 機能要件

### 5.1 対戦モード(v1.0のコア機能)

#### 5.1.0 モードの分け方(2026年8月8日決定)

機能名(ルーム作成/参加)ではなく、**「誰と遊ぶか」**で分ける。

| 表示 | 中身 | 通信 |
|---|---|---|
| **ひとりで** | CPU 1〜3体と対戦。強さを選べる | 不要 |
| **オンライン** | ルーム作成(コード発行・**招待リンク共有**)/ コード入力で参加 / フレンドの招待から参加 | 必要 |

- 「ボット対戦」という開発用語はやめ、**対戦モードの1つ**として正面に置く。ユーザーがいない時期はこれが主役になるため
- **招待リンク共有**:ルームコードをLINE等で送ると、受け取った相手がApp Storeからインストールしてそのまま参加できる導線。友達を呼ぶ手間を減らすと同時に、ダウンロードを増やす狙い
- **1台を回して遊ぶ方式は採用しない**(§4。1ダウンロードで複数人が遊べてしまい、ユーザー数が伸びないため)

#### 5.1.1 ルーム機能

- ホストがルームを作成すると**参加コード(4〜6桁)**が発行される。
- 参加者はコードを入力して入室する(同じ部屋の友達も遠隔の友達も同一方式)。
- ルーム内で対戦設定(ジャンル・問題数・制限時間)を確認でき、ホストが開始する。
- 想定同時対戦人数:2〜8人(上限は技術検証後に確定)。

#### 5.1.2 早押し判定仕様

**標準は「文字送り型」**とし、設定で従来の「速答型」にも切り替えられる。どちらが受け入れられるかはリリース後の反応で判断するため、片方に決め打ちしない。

##### 文字送り型(標準)

「早押しボタンを押してから答えを考える」のではなく、**答えが分かった瞬間にその選択肢を押す**構造。

- 英単語を**1文字ずつ一定間隔で表示する**(例:a → ab → aba → aban → …)
- **4択は問題開始時から全プレイヤーに表示する**。早押しボタンは無い
- **選択肢をタップする操作が「早押し+回答」**。押した瞬間に回答内容と回答時刻が確定し、以後変更できない
- 先着は**サーバー到達順**で決まる(通信遅延による差は仕様として許容)
- **最初に正解したプレイヤーがその問題の勝者**(+1ポイント)
- **誤答したプレイヤーはその問題に再回答できない**(−1ポイント)。他のプレイヤーは引き続き回答できる
- 全員が答え終えて正解が出なかった場合、待たずに発表へ進む
- 誰も正解しないまま制限時間が経過した場合、その問題は流れて次の問題へ
- 回答時には「選んだ選択肢・回答時刻・回答時点の表示文字数」を保持する(何文字目で分かったかの記録)
- **文字の表示間隔は定数で管理**し、後から調整できるようにする(体感の調整箱)

##### 速答型(選択可・従来方式)

- 単語を全部表示した状態から、任意のタイミングで早押しボタンを押せる
- 最初に押した1人に回答権が与えられ、その間ほかのプレイヤーの操作はロックされる
- **選択肢は回答権を得た本人にのみ表示する**
- 不正解の場合、回答権は次に早く押していたプレイヤーへ移行する(押下順キュー)。誤答者はその問題に再度押せない
- 押下者が全員誤答した場合は、制限時間を仕切り直して早押し受付に戻す

##### 共通

- **正解:+1ポイント/不正解:−1ポイント**
- ホストが選んだ形式が全員に適用される(試合中の変更はできない)
- 表示の進み具合は問題の開始時刻からの経過時間だけで決まるので、全端末で同じ見え方になる

進行の時間(文字送りの間隔・回答の制限時間・発表時間など)の**数値はこの文書に書かない**。唯一の出典は `Services/Battle/BattleRules.swift` と `ProgressiveReveal.characterInterval`(体感調整で頻繁に変わるため、文書に写すと必ずズレる)。

#### 5.1.3 試合ルール設定(ホストが変更可能)

| 設定項目 | 内容 | デフォルト |
|---|---|---|
| 問題数 | 選択式(5 / 10 / 15 / 20問) | 10問 |
| 1問あたり制限時間 | 選択式(10 / 20 / 30秒) | 20秒 |
| ジャンル | 英単語(v1.0)/以降拡張 | 英単語 |

自由入力ではなく選択式にしたのは、極端な設定(1問・300秒など)で対戦が成立しなくなるのを防ぐため。

- 最終順位はポイント順。**同点は同順位**とする。

### 5.2 回答形式(出題エンジン)

複数の回答形式をサポートする拡張可能な設計とする。
※「回答形式」=答え方(4択/入力式)。「出題形式」=問題文の見せ方(速答型/文字送り型、§5.1.2)。別の軸なので混同しないこと。

| 形式 | 内容 | 導入時期 |
|---|---|---|
| 4択 | 選択肢から1つ選ぶ | v0.5 |
| 入力式 | テキスト入力で回答(表記ゆれ・大文字小文字の正規化が必要) | **v1.5へ延期**(v1.0はスキーマ上の受け皿のみ) |
| その他(並べ替え等) | 将来拡張 | v1.5以降 |

- 問題データは形式(type)フィールドを持つ共通スキーマで管理し、形式の追加が既存データに影響しない構造とする。

### 5.3 一人練習モード(v0.5)

- 通信なしで、端末内の問題データから出題。
- 出題エンジン(`QuizSession`)を対戦と共通化する。画面は練習用に別途持つ(早押しの競り合いがないため)。練習では単語を全部表示する(文字送りは対戦の駆け引きのための仕組みなので使わない)。
- 間違えた問題は自動で**復習リスト**に登録され、復習モードで再出題できる。

### 5.4 学習履歴・結果表示

- 試合終了後にリザルト画面(順位、正答数、獲得ポイント)を表示。
- 対戦・練習の正誤履歴をSwiftDataに保存し、間違えた問題を復習リストに反映する。

### 5.5 コンテンツ(v1.0時点)

- **英単語**(自作データ)からスタート。単語と意味の対応データは著作権リスクが低く、作成コストも低い。
- 1語につき「単語・品詞・意味・カテゴリ・難易度・説明文」を持つ。出題は「単語 → 意味を4択」の1種類で、**説明文は現在の出題では使わない**(ジャンル追加や入力式で使えるよう保持している)。分類の使い方は §5.10。
- 資格・SPI系は**オリジナル問題(SPI「風」問題等)**として順次追加。市販問題集・過去問の転載は行わない(著作権対応、§7.3参照)。

### 5.6 フレンド機能(v1.0スコープ内)

- 毎回コードを教え合わずに友達を呼べるようにするための機能。**v1.0の必須機能とする**。
- ユーザーごとに固定のフレンドコードを発行し、コード入力で相手をフレンドに追加できる(相互承認は求めない片方向方式)。
- ロビーからフレンドへルーム招待を送れ、受け取った側はワンタップで入室できる。

### 5.7 CPU対戦(v1.0スコープ内)

- 通信なしで対戦できるモード。目的は3つ:**①ユーザーが少ない時期の主役 ②友達がいない隙間時間の練習 ③Firebase未設定・オフラインでも対戦画面を開発・検証できる**。
- 対戦ルールはオンライン対戦と同一とし、結果は一人練習として学習履歴・復習リストに反映する。
- UI上は「ボット対戦」ではなく**対戦モードの「ひとりで」**として見せる(§5.1.0)。

### 5.8 プロフィール(v1.1)

- ニックネーム・アイコン・フレンドコード・戦績・**学習量**を1画面にまとめる。
- フレンドのプロフィールから対戦に誘える導線を持つ。
- フレンドの学習量は**受動的に見せるだけ**とし、投稿・コメントの機能は作らない(§4 の「作らないと決めたもの」)。

### 5.9 スタンプ(v1.1)

- 対戦中・リザルトで送れる定型リアクション。テキストチャットの代わりであり、**自由入力は持たせない**。
- 盛り上がりを作る目的なので、種類は絞る(煽り表現は入れない)。

### 5.10 問題のカテゴリ・難易度分け(基盤はv1.0で実装)

- カテゴリと難易度は別々に管理する。現在のカテゴリは**中学英単語(`junior_high`)**と**高校英単語(`high_school`)**だけとし、大学英語カテゴリは作らない。
- 各カテゴリはそれぞれ `difficulty: 1...5` を持つ。高校を6〜10に通し番号化せず、中学・高校ともに★1〜5として表示する。
- 出題時はカテゴリと難易度の完全一致で抽出する。一人練習・CPU対戦・オンライン対戦で選択でき、オンラインではホストの設定を参加者へ共有する。
- 将来はTOEIC・TOEFL・英検・IELTS等を目的別カテゴリとして追加できる設計にするが、現時点では実装しない。
- 既存200語はテストデータとして2ファイルへ暫定移行し、たける・トキヤが本番データへ順次刷新する。

### 5.11 作問(v1.5)

- 自分で単語と意味を登録し、自分の問題セットとして出題できる。
- **公開範囲は「自分用」と「フレンドへの直接共有」までとする。** 不特定多数への公開はユーザー生成コンテンツ扱いとなり、通報・ブロック・モデレーション体制の実装が必須になるため(§4)。

---

## 6. 非機能要件

| 項目 | 要件 |
|---|---|
| 対応OS | iOS 17以降(SwiftData要件に準拠) |
| 対応端末 | iPhone(iPadは当面スコープ外) |
| リアルタイム性 | 早押し判定の体感遅延が対戦の成立を損なわないこと(目標:押下→判定反映まで概ね500ms以内、技術検証で確定) |
| 切断耐性 | 対戦中の一時切断時に再入室できること(v1.0では最低限、途中離脱者を除いて試合続行できればよい) |
| オフライン動作 | 一人練習モード・CPU対戦は完全オフラインで動作 |
| 音・触覚 | 効果音とハプティクスで早押しの手応えを出す。教室・電車での利用を想定し、マナーモード時は鳴らさず、設定で個別にOFFにできること |
| プライバシー | アカウントは匿名(ニックネームのみ)から開始。個人情報の収集は最小限とする |
| コスト | Firebase無料枠(Sparkプラン)内で開発・小規模運用が完結すること |

---

## 7. 技術構成

### 7.1 アーキテクチャ概要

```
[iPhone (SwiftUI)]
   ├─ SwiftData …… 学習履歴・復習リスト・ローカル問題データ(一人練習用)
   └─ Firebase SDK
         ├─ Realtime Database …… ルーム状態同期・早押し判定(トランザクション)・出題データの配信
         ├─ Firestore …… ユーザープロフィール・フレンド・ルーム招待
         ├─ Cloud Functions …… 採点・進行制御などサーバー権威処理(v1.5で検討)
         └─ Firebase Auth(匿名認証)
```

問題データはサーバーに問題マスタを置かず、**端末内のデータからホストが選んでルームへ配信する**方式にした(v1.0)。オフラインの一人練習と同じデータを使えて、Firestoreの読み取り回数も増えないため。ジャンル追加でデータ量が増えたら問題マスタ配信へ移行を検討する。

### 7.2 技術選定と理由

| 領域 | 選定 | 理由 |
|---|---|---|
| UI | SwiftUI | 標準・開発効率・既存経験(Buffito) |
| ローカルDB | SwiftData | iOS標準、学習履歴・復習リストの管理に適する |
| バックエンド | **Firebase** | Realtime DBのトランザクションで「最初に押した1人に回答権を付与」を原子的に実装可能。情報量・無料枠・将来のランダムマッチ拡張性で優位 |
| 認証 | Firebase匿名認証 | 登録の手間なく開始でき、将来アカウント連携に昇格可能 |

**不採用案:** Supabase(早押しの原子的判定を自作する必要があり、Swift SDK・情報量で劣る)、Game Center(P2P型で判定の権威が持てず、チート・切断に弱い。ルームコードUIの自由度も低い)。

### 7.3 リスクと対応

| リスク | 内容 | 対応 |
|---|---|---|
| 著作権 | SPI・資格の市販問題/過去問の転載は侵害リスク | オリジナル問題のみ使用。英単語から開始。ユーザー投稿導入時は監視体制を設計 |
| 早押しの公平性 | 通信遅延により押下順に有利不利が生じる | サーバー到達順判定を仕様として明記。極端な遅延ユーザーへの表示上の配慮は将来検討 |
| リアルタイム実装難度 | 早押し判定・進行同期が本アプリの技術的最難関 | v1.0着手前に**技術検証(スパイク)**として2台実機での早押しプロトタイプを最優先で作る |
| Firebaseコスト | ユーザー増加時の従量課金 | 無料枠監視。Realtime DBの書き込み頻度を設計段階で抑制 |

---

## 8. データモデル(概要)

### 8.1 SwiftData(端末側)

- **Question**(ローカル問題):id、ジャンル、回答形式(4択/入力)、問題文(単語)、選択肢(意味)、正答
  - 出題形式(速答型/文字送り型)は**見せ方の違い**なので問題データは1セットだけ持つ
- **AnswerRecord**(解答履歴):問題id、正誤、日時、モード(練習/対戦)
- **ReviewItem**(復習リスト):問題id、間違い回数、最終出題日時

### 8.2 Firebase(サーバー側)

- **rooms/{roomId}**(Realtime Database):参加コード、ホストid、参加者一覧、設定(問題数・制限時間・ジャンル)、状態(待機/出題中/リザルト)、その試合の出題データ
- **rooms/{roomId}/game**(Realtime Database):現在の問題番号・進行フェーズ・回答(文字送り型は `answers/{uid}`、速答型は押下キュー `buzz`)。先着はサーバータイムスタンプで決まる
- **users/{uid}**(Firestore):ニックネーム、フレンドコード。配下にフレンド一覧とルーム招待

※実際のフィールド構成は `FIREBASE_SETUP.md` の「データ構造(実装済み)」が正。

---

## 9. 画面構成(v1.0)

**タブは3本**(2026年8月8日決定)。従来の5本(ホーム/ルーム/フレンド/復習/設定)は役割が重複し、使用頻度の低いものがタブを占有していたため統合した。

| タブ | 役割 | 含む画面 |
|---|---|---|
| **対戦**(起動時) | アプリの主役。誰と遊ぶかを選ぶ | モード選択(ひとりで / オンライン)/ CPU対戦の設定 / ルーム作成・参加 / 待機ロビー / 対戦画面 / リザルト |
| **学習** | 一人で積み上げる場所 | 一人練習(設定 → 出題 → リザルト)/ 復習リスト / 習熟度(v1.1) |
| **マイページ** | 自分と友達、アプリの設定 | プロフィール / フレンド一覧・追加 / 設定 / お知らせ / プライバシーポリシー・利用規約 / 作問(v1.5) |

方針:

- **設定は画面右上の歯車ではなくマイページに集約する**。入口を1つにして迷わせない
- お知らせ・ポリシー・利用規約もマイページに置く(審査で必要になる導線でもある)
- 起動時は**対戦タブ**を開く。学習アプリだが、開いた瞬間に遊べることを優先する

---

## 10. 未決定事項(今後の検討)

| # | 項目 | 状態・決定時期の目安 |
|---|---|---|
| 1 | アプリ名 | **決定済み(2026年8月4日):「マナビート」**(サブタイトル「友達と早押し勉強対戦」)。**Bundle ID `com.n.HayaosiApp`・リポジトリ名・ターゲット名は変更しない**(App Store上で別アプリ扱いになるため。表示名のみ差し替える)。アイコンは8/6までに用意 |
| 2 | 入力式回答の正誤判定ルール(表記ゆれ許容範囲) | v1.5(入力式の導入自体をv1.5へ延期。§5.2) |
| 3 | 同時対戦人数の上限 | **暫定確定:8人**。実機検証で見直す |
| 4 | 押下→判定反映のレイテンシ目標値 | **暫定確定:500ms**(§6)。実機2台の検証で確定する |
| 5 | 収益化方針 | **方針決定済み(§4.2):問題パックの買い切り。v1.0では実装しない**。実施時期はユーザーが付いてから |
| 6 | ランダムマッチのマッチング方式・レート設計 | v1.5企画時。**同時接続数が確保できるまで着手しない**(空室になるため) |
| 7 | 早押しの駆け引き方式 | **決定済み(2026年8月3日):標準は文字送り型・設定で速答型に切替可(§5.1.2)。どちらを主にするかはリリース後の反応で見直す** |
| 8 | 配点を早押しの速さで変える案(§5.1.2 案C)の採否 | v1.5以降。±1点ルールの変更がリザルト・履歴に波及するため、まず文字送り型の反応を見てから |

---

## 11. 直近のアクション

最新の進捗と担当は `STATUS.md` を参照(この節は方針レベルの残作業のみ)。

1. **技術検証スパイク:** 実機2台で「押下→先着1名に回答権付与」の判定精度と体感遅延を確認する(Firebaseセットアップ完了後)
2. セキュリティルールの本格設計(リリースブロッカー。`FIREBASE_SETUP.md` §4-1参照)
3. 文字送り型の体感調整(表示ペース・制限時間の初期値)を実機で確認する
4. 英単語データの品質見直し・拡充(現状100語)
