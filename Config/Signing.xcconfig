// 署名設定。開発者ごとに違うチームIDは Config/local.xcconfig(git管理外)で与える。
// 用意していなくてもSimulatorビルドは通る(実機ビルド時だけ必要)。
// セットアップ: cp Config/local.xcconfig.sample Config/local.xcconfig して自分のチームIDを書く
#include? "local.xcconfig"
