# HayaosiApp 開発アーカイブ

完了した過去の作業記録と、終わった計画の置き場所。**日々の開発でこのファイルを読む必要はない**(「あの修正はいつ・なぜやったか」を調べるときだけ読む)。
現在の状態・宣言・次のタスクは `STATUS.md` が正。

---

## 過去の更新(2026年9月3日・整理分)

- Codex(たける側): **代名詞・接続詞・前置詞を誤答生成時だけ同一グループへ統合**。データの品詞7種は維持し、動詞・名詞・形容詞・副詞は従来どおり個別。少数3品詞が将来各4語以上になれば分離できる意図を`PartOfSpeech`へ記録した。変換ツールの最低語数検証も同じグループ単位へ変更し、`python3 word_bank/word_bank_to_json.py`はエラーなしで完了(中学17語・高校183語は変更なし、TOEIC400語は更新あり)。`--write`なしのためJSONは未更新。Simulator向けビルド成功、少数3品詞だけで4択を作り名詞が混ざらない回帰テストを含む全75テスト成功
- Codex(たける側): **作業用Excel 4ファイルと変換ツールを`word_bank/`へ集約し、TOEICをシート別IDへ変更**。銀のフレーズは`tc1_`、金のフレーズは`tc2_`を期待し、空欄IDは接頭辞ごとに独立採番する。ID重複は完全一致、単語重複は従来どおりカテゴリ全体で検出。Excelは移動前後のSHA-256一致を確認し内容未変更。`python3 word_bank/word_bank_to_json.py`ではID形式エラー0件で、別件の代名詞1語・前置詞1語だけを検出。独立採番・接頭辞別形式エラー・重複判定の回帰確認と構文確認に成功。品詞不足が残るためJSONは未更新
- Codex(たける側): **英単語の品詞を7種へ拡張**。`PartOfSpeech`へ副詞・代名詞・接続詞・前置詞を追加し、変換スクリプトの許容値・エラー表示も7種へ更新。全カテゴリ合計で使用中の品詞が1〜3語なら4択を作れないため、品詞名と語数を示して停止する検証を追加した(未使用の0語は対象外)。`Question`は品詞を保持しないためSwiftData移行は不要。独立検証、Simulator向けビルド、全72テスト成功。**作業用`word_bank_toeic.xlsx`は入力途中のため変換確認未完了**:difficulty空欄47件、ID重複2件、接続詞3語を検出。ブックは変更していない
- Codex(たける側): **TOEIC単語20語を`word_bank_toeic.xlsx`へ分離**。銀のフレーズへdifficulty 1・2の8語、金のフレーズへ3・4・5の12語を移し、`word_bank.xlsx`は使い方・中学17語だけに変更。変換スクリプトはTOEICの2シートを通したID採番・単語/ID重複確認、シート別difficulty検証、4ファイルの不足表示へ対応した。変更した2ブック全4シートを目視確認し、`python3 word_bank/word_bank_to_json.py`で3つのJSONすべて「変更なし」を確認。一時移行スクリプトは削除済み

---

## 過去の更新(2026年8月24日・追加整理分)

- Codex(たける側): **アプリを起動したまま自然に期限切れしたサブスクを、フォアグラウンド復帰時に再確認するよう修正**。`HayaosiAppApp`で`scenePhase`を監視し、`.active`になるたび`SubscriptionService.refreshEntitlements()`を呼ぶ。初回起動の`start()`と取引更新監視は維持し、同メソッドを`internal`へ変更。XcodeGen生成、Simulator向けビルド成功、iPhone 17 Simulatorで全72テスト成功。**未検証**:StoreKitローカル購入を実際に自然失効させてからのバックグラウンド→フォアグラウンド通し操作
- TOKIYA-YAMAMOTO(Codex): **マイページ上部プロフィールカードのDark Modeコントラストを改善**。カード内パネル・アイコン縁・境界線・フレンドコード背景をLight/Darkで自然に切り替わるシステムカラーへ変更し、戦績数値とフレンドコードへ明示的な主文字色を適用。レイアウト・機能・Firebase・戦績ロジック・フレンド欄・設定・広告・下部タブは変更していない。Simulator向けビルド成功。iPhone 17 Pro SimulatorでLight/Darkを各1回確認し、両表示でプロフィールカード内の文字が明確に読めることを確認。全テストと実機表示は未検証
- TOKIYA-YAMAMOTO(Codex): **マイページを参考画像に沿ったゲームプロフィールUIへ刷新**。アイコン・名前・自己紹介を主役にしたプレイヤーカード、右上から既存プロフィール編集へ直接進む導線、対戦数・1位回数・1位率の3列、カード下部のフレンドコード・コピー・共有、取得済みプロフィールだけを使う横並びフレンド概要、サウンド・振動／学習データ／お知らせ／アプリ情報を統一カードへ整理。既存の規約・ポリシー・作問導線はアプリ情報内へ維持。試合単位の順位を持つローカル履歴がまだ無いため戦績は捏造せず「—」表示とし、Firestore・RTDB・Presence・XP等は追加していない。XcodeGen生成、Simulator向けビルド、全71テスト成功。iPhone 17 Pro Simulatorで最終レイアウト、1行のコピー・シェア、下部3タブと既存バナー広告の維持を確認。各遷移先でのタップ操作と実機表示は未検証
- Codex(たける側): **StoreKit 2の広告非表示月額サブスク土台を実装**。商品IDは`com.n.HayaosiApp.premium.monthly`の1本のみ。起動時の権利確認・取引更新監視・購入・復元、マイページのプレミアム画面を追加し、購読中はバナーの高さを持たず、全画面広告の読み込み・表示も行わない。`Config/Subscription.storekit`をXcodeスキームへ設定し、仮価格は月額300円。日本語→英語モード・将来パック解放は未実装。xcodegen実行、Simulator向けビルド成功、StoreKit設定検証を含む全72テスト成功。**未検証**:StoreKitローカル購入・復元・失効時の画面操作と広告消去の通し確認。App Store Connectの商品・実価格・契約情報は別途たけるが設定する
  - **2026年8月24日:マイページUI刷新(`491638e`)とのマージを解消**。プレミアム導線は右上ツールバーから「設定・その他」カードの最上段へ移動(右上は刷新版の「編集」1つに戻した)。xcodegen実行、Simulator向けビルド成功、全72テスト成功。iPhone 17 Simulatorでマイページの表示を確認

---

## 過去の更新(2026年8月26日・整理分)

- TOKIYA-YAMAMOTO(Codex): **フレンド画面を「フレンド／送信済み／受信中」の3一覧へ整理**。送受信双方で相手名・フレンドコードを表示し、送信時に受信申請と送信控えを同じFirestoreバッチで作成、承認・拒否時に同時削除する。送信控え導入前の片側だけの申請は、同じコードの再入力で欠けた文書だけを補完する。Local EmulatorのRulesテスト全24件、Swiftテスト全71件、Simulator向けビルド成功。**本番Firestoreルールは未公開で、実在Firebaseと実機2台は未検証**
- Codex(たける側): **3タブ上部の大きな見出しと対戦ステータス表示を削除**(たけるの指示)。`BattleHubView`・`StudyHubView`・`MyPageView`の`navigationTitle`と、対戦タブの連続日数・今日の回答数の行を削除。使われなくなった`BattleStatusRow.swift`・`LearningActivitySummary.swift`とそのテストも削除し、`docs/CODING.md`のテスト方針から集計の記載を外した。タブバーのラベルと対戦タブ右上の「遊び方」は維持。xcodegen実行、Simulator向けビルド成功、全66テスト成功。iPhone 17 Simulatorで3タブを切り替え、各見出しと不自然な上部余白・ナビゲーションバーの残骸がないこと、「学習メニュー」とタブラベルが残ること、「遊び方」から体験画面が開くことを確認。CPU対戦も4問をロビーからリザルトまで完走
- TOKIYA-YAMAMOTO(Codex・たける側代行): **初回ユーザー向けに1問だけの対戦ルール体験、リザルトから今回間違えた問題だけを既存の復習機能で解き直せる導線、フレンド操作の完了・確認表示を追加**。文字送りを見て分かった瞬間に4択を押し、そのタップが早押しと回答を兼ねることを、実際の対戦と同じ表示部品で体験できる。初回のみ自動表示し、以後も対戦タブ右上の「遊び方」から再表示可能。復習はCPU・オンライン共通で、自分が誤答した問題がある場合だけリザルトに表示する。フレンド申請後は相手の承認待ちであることを表示し、相互削除前に確認し、削除失敗時は画面へエラーを返す。Priority 0の実機2台検証は相手端末が現在利用できないため保留し、独立して進められるPriority 1・2・4を先行した。相手の最新UI整理を統合後、XcodeGen生成、Simulator向けビルド・起動、全68テスト成功。初回画面と通常起動はiPhone 17 Pro Simulatorで確認。**チュートリアルの正解・誤答後操作、リザルトから復習する操作、実在Firebaseでの申請完了・削除確認、実機表示は未検証**。Firebaseのデータ構造・セキュリティルール・対戦ルール・得点ルールへの変更なし
- TOKIYA-YAMAMOTO(Codex): **通信対戦で前問の回答・お手付きが次問や別プレイヤーへ混ざる競合を修正**。回答とお手付きへ問題番号を付け、現在の問題と一致するデータだけを復元し、ホストの採点・問題送りのFirebase書き込みを直列化。RTDBルールでは現在の問題番号と違う回答・お手付きを拒否する。CPU対戦にも前問の予約回答が次問へ混ざらない回帰テストを追加。また、CPU対戦・ルーム作成のジャンル欄を、反応しない固定selectionのPickerから「英単語」の固定表示へ変更。Swiftテスト全72件、Local EmulatorのFirestore/RTDB Rulesテスト全22件、Simulator向けビルド・起動に成功。**2026年8月21日に本番RTDBルールを公開し、公開後に本番ルールがローカルの修正版と完全一致することを確認。修正版での実機2台通信対戦は未検証**
- TOKIYA-YAMAMOTO(Codex): **フレンド申請・同時誤答・回答送信失敗を改善**。フレンドは申請を相手が承認すると双方へ原子的に登録し、拒否・相互削除・既存の片方向フレンドからの移行に対応。回答送信失敗時は端末側の回答済み状態を解除して再送可能にし、次の問題まで回答不能になる状態を防止。同時に近い誤答でも本人の不正解表示を一定時間維持する。Swiftテスト全70件、Local EmulatorのFirestore/RTDB Rulesテスト全20件に成功し、2026年8月17日に本番Firestoreルールを公開。**新方式の実機2台確認は未実施**
- TOKIYA-YAMAMOTO(Codex): **RTDB本格ルールと実装済み通信経路の不整合を修正**。未使用ルームコードの確認読み取り、待機中の本人入室、対戦中の本人退出、参加者本人の初回回答、ホストの進行・得点更新を許可し、対戦開始後の途中参加、他人の回答、再回答、部外者、回答フェーズ外、不正な回答形式を拒否。ルーム作成→ゲスト入室→開始→回答→採点→終了を含むLocal EmulatorのRulesテスト全17件成功。2026年8月17日に本番RTDBルールを公開し、Firebase Console再読込後も修正版3条件の反映を確認。**実機2台は未検証**
- TOKIYA-YAMAMOTO(Codex): **プロフィールのicon・bioをFirestoreへ同期**。`users/{uid}`への保存と新規フレンド文書へのコピーに対応し、ルールでアイコンをプリセット20種または空文字、自己紹介を140文字以内に制限。既存フレンドも一覧表示時に各プロフィールを1回取得して最新表示へ補完。Local EmulatorのRulesテスト12件、Swiftテスト70件、Simulator向けビルドに成功。2026年8月17日にFirestore本番ルールを公開し、iPhone 17 Pro Simulatorで6桁コード取得、`users/{uid}`のnickname・friendCode・icon・bio更新、`friendCodes/{code}`索引、空のフレンド一覧表示を実在Firebaseで確認。**実機2台・通信対戦は未検証**。常時監視とフレンド文書への書き戻しは行わず、一覧を開くたびフレンド人数分の追加読み取りが発生し、画面を開いたままのプロフィール変更は次回表示まで反映されない

---

## 過去の更新(2026年8月25日・整理分)

- Codex(たける側): **高校英単語183語を担当者別・ターゲット別の作業用xlsxへ再編**。`word_bank_takeru.xlsx`へターゲット1200(difficulty 1・2、27語)とターゲット1400(3、60語)、`word_bank_tokiya.xlsx`へターゲット1900(4・5、96語)を移し、`word_bank.xlsx`は使い方・中学17語・TOEIC20語だけに変更。変換スクリプトは高校3シートを通したID採番・単語/ID重複確認、シート別difficulty検証、3ファイルの不足表示へ対応した。3ブック全6シートを目視確認し、`python3 word_bank/word_bank_to_json.py`で3つのJSONすべて「変更なし」を確認。一時移行スクリプトは削除済み

## 過去の更新(2026年8月24日・整理分)

- Codex(たける側): **アプリを起動したまま自然に期限切れしたサブスクを、フォアグラウンド復帰時に再確認するよう修正**。`HayaosiAppApp`で`scenePhase`を監視し、`.active`になるたび`SubscriptionService.refreshEntitlements()`を呼ぶ。初回起動の`start()`と取引更新監視は維持し、同メソッドを`internal`へ変更。XcodeGen生成、Simulator向けビルド成功、iPhone 17 Simulatorで全72テスト成功。**未検証**:StoreKitローカル購入を実際に自然失効させてからのバックグラウンド→フォアグラウンド通し操作
- TOKIYA-YAMAMOTO(Codex): **マイページ上部プロフィールカードのDark Modeコントラストを改善**。カード内パネル・アイコン縁・境界線・フレンドコード背景をLight/Darkで自然に切り替わるシステムカラーへ変更し、戦績数値とフレンドコードへ明示的な主文字色を適用。レイアウト・機能・Firebase・戦績ロジック・フレンド欄・設定・広告・下部タブは変更していない。Simulator向けビルド成功。iPhone 17 Pro SimulatorでLight/Darkを各1回確認し、両表示でプロフィールカード内の文字が明確に読めることを確認。全テストと実機表示は未検証
- Codex(たける側): **難易度別正答率セルを56pt・線幅6ptの円環プログレスリングへ変更**。12時位置から時計回りに進捗を描き、中央へ正答率、下へ★1〜★5を表示。既存の緑80%以上/黄55〜79%/赤54%以下/データ無しグレー、5列の等間隔、タップ挙動、問題0件時のopacity 0.4と遷移無効化は維持。xcodegen実行、Simulator向けビルド成功、全70テスト成功。検証専用iPhone 17 Proへ100%/66%/33%/0%/未回答の一時履歴を入れ、ライト/ダーク双方で緑・黄・赤・背景グレー・未回答を識別できることを確認後、一時履歴を削除。スクリーンショットは`docs/screenshots/difficulty-rings-light.png`と`difficulty-rings-dark.png`。本作業ではFirebase関連ファイル・本番環境を変更していない
- TOKIYA-YAMAMOTO(Codex): **本番設定を維持した開発者別Bundle ID・Signing・Firebase構成へ更新**。共有既定値は本番Bundle ID `com.n.HayaosiApp`、共同開発者はgit管理外の`Config/local.xcconfig`だけでPersonal Teamと開発用Bundle IDを上書きする。2つのiOSアプリは同じFirebaseプロジェクトを共有し、各自が実効Bundle IDに一致するgit管理外のplistを使用する。Firebaseプロジェクト`hayaosiapp`へ開発用iOSアプリ`com.n.HayaosiApp.dev.tokiya`を登録し、対応plistとPersonal Teamをローカルへ設定。XcodeGen生成、実効Bundle ID・Teamの確認、Simulatorビルド、Personal Teamでの実機署名・iPhoneへのインストール・起動に成功。**実機上のFirebase疎通と実機2台通信は未検証**。本番Bundle ID・本番Firebaseルール・既存データは変更していない
- Claude(たける側): **bundle ID変更は見送り、Apple Developer Supportへ削除依頼を送付**(たけるの判断)。`com.n.HayaosiApp`は変更しない。無料Personal Team(atokiya@icloud.com)側の一時保持がApple公式サポートで早期解放されるかの返信待ち。返信が来るまで有償チーム(LL98RL72H4)での実機ビルドは引き続き不可。**これは当時の記録であり、現在は上記の開発者別Bundle ID方式へ移行**
- Claude(たける側): **効果音の実音源への差し替え**。正解・不正解・時間切れ・出題の4種を配布素材へ差し替え、決定ボタン音を新規追加(`se_button.wav`)。詳細は下記Codexの実装記録と合わせて参照
  - `se_correct.wav`/`se_wrong.wav`:「Quiz-Ding_Dong02」「Quiz-Buzzer02」シリーズより選定
  - `se_timeup.wav`:「Countdown06」よりカウントダウン末尾のブザー部分のみ切り出し(頭のカウント部分は発表タイミングと合わないため不使用)
  - `se_button.wav`:「決定ボタンを押す31」より無音部分を除去して0.18秒に整形
  - `se_question.wav`も「Quiz-Question01」へ差し替え済みだが、**Codex実装で出題音の発火自体が削除されたため現在未使用**(たけるの判断でこの方針を採用。ファイルは将来の再利用に備えて残置)
  - 各素材のライセンス(商用利用・クレジット表記の要否)は**たけるが配布元で確認済み**
  - 検証:ビルド成功・テスト59件パス。**音の聴取確認とSimulator通し操作はこの環境では未検証**
- Codex(たける側): **画面遷移・決定操作に決定音を追加**
  - `SoundPlayer.Effect` に `se_button` を追加。見た目を変えない共通の `SoundButtonStyle` を新設し、「ひとりで」「オンライン」、ルーム作成/参加、練習開始、ロビー開始など指定の画面遷移・決定操作だけへ適用。戻る・閉じる・トグル・招待など対象外には適用していない
  - 出題音は対戦・一人練習の両方から削除し、対戦中の4択は触覚フィードバックだけを残して回答タップ音を削除。既存の効果音ON/OFF設定に従う
  - 検証:Simulator向けビルド成功、テスト59件成功。**Simulatorの通し操作と音の聴取はこの環境では未検証**(実機での音量バランス確認も人間担当)

- Claude(たける側): **表示名を「マナビート」→「スタはや」に変更**(たけるの指示)。Bundle ID・リポジトリ名・ターゲット名は不変(`project.yml` の `INFOPLIST_KEY_CFBundleDisplayName` のみ変更)
  - 招待文面2件(`BattleLobbyView`・`FriendProfileCard`)を新名称に修正。CLAUDE.md・README.md・要件定義書のアプリ名表記を更新。**アイコンは名称変更前のものを継続使用**(差し替えは別タスク)
  - `STATUS_ARCHIVE.md` は過去の決定記録なので不変(当時「マナビート」に決めた事実として残す)。**J-PlatPatでの商標確認は新名称で完了(2026年8月22日・たける)**
  - 検証:テスト59件パス。表示のみの変更なのでSimulator確認は未実施(ロジック変更なし)

- Claude(たける側): **回答した瞬間に得点と正誤を反映し、答える人が残っていなければ待たずに発表へ進むようにした**(たけるの指示)
  - **回答のたびに、その時点で確定した得点へ置き直す**。毎回ゼロから計算し直すので、後から順位が入れ替わっても二重加算にならない。誤答の−10も回答した瞬間に反映される
  - オンラインはホストが問題中も `players/*/score` を更新する。現在値と違う項目だけを書くので、自分の書き込みで観測が再帰しない。**RTDBのスキーマは変更していない**(トキヤ氏へ:ホストの書き込み頻度だけが上がる)
  - CPU対戦は、参加を見送ったCPUを待ち対象から外した(以前は不参加CPUが1体でもいると必ず制限時間まで待っていた。CPU7体なら7割以上の問題が該当)
  - 出題中も確定した正誤を○/×で表示する(得点が同時に動くため、正誤だけ伏せても意味がない)。正解音は発表時ではなく回答時に鳴らし、発表では誰も取れなかった時だけ時間切れ音を鳴らす
  - 検証:テスト59件パス(即時加点・即時減点・順位の置き直し・不参加CPUを待たない、の4件を追加/書き換え)。**Simulatorでの通し確認とオンライン実機2台は未検証**

---

## 過去の更新(2026年8月11日・整理分)

- Claude(たける側): **制限時間の既定を5秒にし、全員が回答し終えたら制限時間を待たずに発表へ進むようにした**(たけるの指示による修正)
  - `QuizDefaults.timeLimit` を5秒、選択肢を5/10/20/30秒に変更(Codex実装時の25秒は取り違え)
  - CPU対戦は `finishQuestion` を時間切れと「全員回答済み」で共有。オンラインもホストが同じ判定で採点し、スナップショットが複数回届いても二重加算しないよう `scoredQuestionIndex` でガード
  - 全員が回答権を使い切ったかは正誤を問わず `game.answers` の顔ぶれで判定する(誤答も回答済みとして数える)
  - 検証:テスト57件パス(「全員回答で制限時間前に発表」1件追加)。SimulatorでCPU対戦4問を完走し、CPU回答時の順位バッジ・×印・並び替え・リザルトまで確認。**オンライン実機2台は未検証**

- Codex(たける側): **対戦を制限時間終了時の一括採点へ変更**。正解者内の回答時刻順で+20/+10/+5/4位以降+1、不正解−10、無回答0とした
  - 正解者が出ても制限時間までは全員の回答を受け付け、CPU・オンライン共通の純粋ロジックで採点。RTDBの `reveal.scorerID/byTimeout` は正解順位順の `correctIDs` へ変更し、複数正解者の○印と発表表示に対応
  - 制限時間の初期値は25秒、選択肢は10/20/25/30秒。検証:ビルド成功、テスト56件パス。SimulatorのCPU対戦通し操作とオンライン実機2台は未検証

- Codex(たける側): **対戦を文字送り型へ一本化し、丸アイコン列のスコアボードへ刷新**。
  - 出題形式の選択UI・旧形式専用の進行処理・RTDB設定を削除。回答順はCPU・オンライン共通で `game.answers` のサーバー時刻だけから決め、誤答者は `game.failed` へ記録する
  - スコアボードはホスト→入室順を基本に、回答した順で左へ並び替える。丸内は人型アイコン、左上に順位、右下に赤い○/×を表示。順位バッジはライト/ダーク両方に追従し、8人時は横スクロールで崩れない
  - CPU対戦はCPUを最大7体(自分を含め8人)まで選択可能にした。検証:ビルド成功、テスト50件パス。Simulator通し操作はこの環境の補助アクセス権不足で未検証

- 人間: **同時押し時に対戦進行が停止する現象を1回確認**。条件の詳細と再現率は未整理。
  - コード上の有力仮説:`OnlineBattleSession+Host` が1件目を採点中、2件目の状態更新を `isEvaluatingAnswer` で見送った後、書き込み完了時に再判定を呼ばないため、未処理回答が残る競合状態
  - 修正前の実機2台検証:文字送り型で「正解+正解」「誤答+正解」「誤答+誤答」を各10回。同じ端末の連打も確認し、両端末の画面録画・ホスト側Xcodeログ・問題番号・選択肢・停止した画面を記録する
  - 修正後は同じ表を再実行し、2件目が必ず処理されること、得点の二重加算がないこと、先着順がサーバー時刻順であること、時間切れまで待たず進行することを合格条件とする

---

## 過去の更新(2026年8月9日・整理分)

- Claude(たける側): **Simulatorスクリーンショットのトークン節約方針をCLAUDE.mdへ追記**(たける発案)。座標探しはread_page/find等を使い撮らない、見た目確認は最終状態のみ上限2枚、ロジックのみの変更は撮らない、重い通し確認はたけるが自分のSimulatorで見る、の4点を「完了の定義」に明文化

- TOKIYA-YAMAMOTO(Codex): **Firebase本格セキュリティルールの目標仕様とLocal Emulator自動テストを追加**。
  - Realtime Database:待機中の部屋だけ参加前の確認を許可し、対戦開始後は参加者のみ読み取り可。ルーム作成はホスト本人、ゲーム進行・得点更新はホストのみ、参加者は自分の参加・退出だけを許可
  - Firestore:`users` と `friendCodes` の一覧取得を禁止。プロフィールとフレンドコード逆引きを同時作成し、招待は送信者が登録済みフレンドへ送る場合だけ許可
  - `firebase.json` / `database.rules.json` / `firestore.rules` / `FirebaseRulesTests/rules.test.js` / Node.js開発専用依存を追加
  - `demo-hayaosiapp` のEmulatorでルールテスト**9件パス**。実在するFirebaseプロジェクトや本番データには接続していない
  - **本番ルールは未変更**。`AuthService` / `FriendService` が `friendCodes/{code}` と招待の `fromUID` に対応するまで公開しない
  - npm監査は中程度6件(すべてローカル開発用Firebase CLIの間接依存)。iOSアプリには同梱されない。自動修正・強制更新は未実施

- TOKIYA-YAMAMOTO(Codex): **Firestore本格ルールへクライアントを対応**。プロフィールと `friendCodes/{code}` を原子的に作成し、既存ユーザーの索引不足・コード競合を安全に補修
  - フレンド追加を索引の直接参照へ変更し、招待へ `fromUID` を追加。Rulesテストへ既存ユーザー移行とクライアント形式の一連操作を追加
  - 検証:Local EmulatorのRulesテスト11件、Swiftテスト59件、Simulator向けビルドすべて成功。**本番ルール・実在Firebase・実機2台は未変更/未検証**


- Codex(たける側): **試合開始前に「対戦開始!」を2秒表示する共通ゲートを追加**。CPU・オンラインとも1問目の前だけ通り、端末時計が進んでいても最低0.5秒は表示する
  - オンラインはRTDBの `game.startDelayMS=2000` とサーバー時刻の `startedAt` から有効開始時刻を算出。CPU回答・時間切れ・オンラインのホストタイマーも同じ開始時刻まで遅らせ、旧ルームは待ち時間0として互換維持
  - 検証:当時の対戦形式をロビー→開始画面→対戦→リザルトまで完走。開始中にCPUが回答しないこと、開始後の残り20秒、全画面のタブバー非表示、退出後の復帰を確認。ビルド成功・テスト48件パス。オンライン実機2台は未検証

- Claude(たける側): **対戦画面UIの強化(第1弾)**。
  - 対戦中はタブバーを隠し、スコアボードでは誤答して回答権を失ったプレイヤーを半透明+✋で表示
  - 検証:テスト42件パス。Simulatorで当時の全形式を通し確認。タブバーが全画面で消えること、CPUのお手つきが表示されることを確認


- Codex(たける側): **英単語を「カテゴリ」と5段階の「難易度」で管理する構成へ移行**。対象カテゴリは中学・高校のみで、大学英語は作らない
  - `junior_high.json` / `high_school.json` に分割し、各語へ `category` と `difficulty: 1...5` を付与。既存200語は入力し直すまでのテストデータとして移行し、IDを `jh_####` / `hs_####` に統一。`QuestionSeeder.dataVersion` は6
  - `Question` と読み込み処理を新構造へ対応。同一ファイル内はID・単語の重複を拒否し、中学・高校をまたぐ同じ単語は許可。既存の `pos` / `definition` は保持
  - 一人練習・CPU対戦・オンライン対戦でカテゴリと★1〜5を選び、完全一致する問題だけを出題。星表示は数値からUI側で生成
  - オンラインはホストがルーム作成時に選択し、RTDBのsettingsへ `wordCategory` / `wordDifficulty` を保存。参加者のロビーにも同じ設定を表示し、ホストが該当問題だけを配信する。分類設定のない旧ルームは従来どおり全問題を対象にする
  - アプリ内の開発用単語追加フォームと書き出し処理は削除。今後は2つのJSONへ、たける・トキヤが直接入力する
  - 旧IDの学習履歴・復習項目は同じ単語の新IDへ引き継ぐ。検証:JSON構造・ID接頭辞・カテゴリ内重複・完全一致抽出・星表示・オンライン設定の往復を含むテスト42件パス、既存データを残したSimulatorへの上書き起動と200件の再投入を確認

- Claude(たける側): **CLAUDE.md を「守るべきルールだけ」に整理**(85行→55行)。**Gitの節を新設**:`main` へ直接コミットしブランチは使わない(担当が分かれていて衝突しにくいため。App Store申請と並行開発が始まったら見直す)、`pull` 後と `xcodegen generate` の関係、コンフリクト時に単語データJSONの相手の追加語を消さないこと、を明文化
  - ディレクトリ構成図とビルド/テストコマンドを `docs/CODING.md` へ移動。**絶対パス `/Users/n/…` を排除**したのでトキヤ氏の環境でもコマンドがそのまま通る。機種名は `simctl` で確認する旨と、一覧にあっても解決できない機種がある注意を追記
  - 担当分担の重複記述を CLAUDE.md から削除(**正は STATUS.md**)。AGENTS.md はルールの再掲をやめ CLAUDE.md 参照だけに縮小(文言のずれを解消)
  - 検証:テスト42件パス(ドキュメントのみの変更でコードは触っていない)
- たける: **担当分担と次の実装順を変更**。英単語コンテンツ・レベル分け・単語追加UI・対戦画面UI・効果音/ボイスをたける側へ、バックエンド全般・追加テスト・既存バグ改善をTOKIYA-YAMAMOTO側へ移管
- Codex(たける側): **マナビートのアプリアイコンを設定**。提供画像を透明なしの1024×1024 PNGへ変換して `AppIcon.appiconset` に登録。XcodeGen再生成・Simulator向けビルド成功でアセットカタログのコンパイルを確認

---

## 過去の更新(2026年8月8日・その2)

- Claude(たける側): **責務分割とCPU対戦のテスト追加(挙動変更なし)**。
  - `CPUBattleSession`(353行)から**CPUの判断だけを `CPUAnswerStrategy` へ抽出**(純粋計算・`random` 注入で固定可能)。`CPUProfile` と強さの値もそこへ移動。進行・採点はprivateな状態遷移そのものなのでセッションに残した(アクセス制御を弱めないため)
  - タイマー生成を `TimerScheduler` として注入可能に(既定は従来と同じ実時間待ち。本番挙動は不変)
  - リザルトの順位計算をViewから `BattleRanking` へ抽出(同点同順位がテスト可能に)
  - **`Tests/CPUBattleSessionTests.swift` を追加(8件)**:初期状態/開始遷移/正答・誤答の採点と再回答ロック/最終問題→リザルト/次問への進行/同点同順位/CPU 0〜99体で完走。手動発火タイマーと固定乱数で実時間ゼロ
  - `FriendService.swift` に同居していた `Friend`・`RoomInvite` を `Services/Online/Models/` へ分離し、`RoomState.swift` も同フォルダへ移動(Firebaseのフィールド名・パスは不変)
  - README・FIREBASE_SETUP・要件定義書§6 の旧用語(ボット対戦/ルームタブ)を現行UIに合わせて修正(STATUS_ARCHIVEは当時の記録なので不変)
  - 検証:`git diff --check` OK・テスト33件パス・SimulatorでCPU対戦1試合完走(文字送り・4択・リザルト表示)

- Claude(たける側): **構造リファクタ(名前と場所の整理。ロジック変更なし)**。たけるの判断で実施(※リネームは本来トキヤ氏の事前合意対象。push時に pull --rebase + xcodegen generate 必須の旨を連絡すること)
  - `Views/Room/` の4ファイルを `Views/Battle/` へ `git mv`(対戦フローを1フォルダに)。`Views/Room/` は消滅
  - 共用Viewから `Online` 接頭辞を除去:`OnlineBattleView`→`BattleView` / `OnlineLobbyView`→`BattleLobbyView` / `OnlineResultView`→`BattleResultView` / `OnlineRoomView`→`BattleFlowView`(いずれもCPU対戦でも使うため名前が実態と乖離していた)。オンライン専用の `OnlineBattleSession`・`OnlinePreviewBanner` は据え置き
  - `Bot`→`CPU` 語彙統一:`BotBattleSession`→`CPUBattleSession`、ID接頭辞 `bot-`→`cpu-`(オフライン専用でRTDBに載らないため保存データへの影響なし)、ニックネーム生成を「CPU(強)」等に変更し、**表示変換シム `BattlePlayerDisplayName` を削除**
  - 検証:旧名の残存0件をgrepで確認、テスト25件パス、Simulatorで CPU対戦を1試合完走(スコア表示・お手つきロック・リザルトの同点同順位まで)+オンラインのシート表示を確認

- Claude(たける側): **STATUS.md に150行の上限を設け、超過を自動検知するフックを追加**(たける発案)。`.claude/hooks/check-status-size.sh` が SessionStart と Edit/Write 後に行数を確認し、超過時は「古い日付の項目からアーカイブへ」と警告する。移動自体は自動化しない(何が「古い」かは文脈判断が要るため、検知=機械/移動=判断側の分担)。4ケースのパイプテストで動作確認済み

- Claude(たける側): **CLAUDE.md を「構造と開発ルールだけ」に再編し、コーディング規約・ナレッジを `docs/CODING.md` へ分離**(たける発案)。常時読み込むのは CLAUDE.md と STATUS.md だけにし、規約はSwiftを書くときだけ読む方式。CLAUDE.md に「読み込みガイド」表を新設。命名規約(View名に通信の有無を入れない/UIとコードの用語一致/用語変更時は同一作業で追従)を docs/CODING.md に追加。AGENTS.md の読み順も更新

- Claude(たける側): **STATUS.md を分割**。完了済みの過去記録と終わった計画を `STATUS_ARCHIVE.md` へ移し、本体は「現在の状態・宣言・次のタスク・決定事項」だけにした(毎セッション読むファイルを太らせない運用。冒頭の運用ルールに明記)。あわせて陳腐化していた記述を修正:決定事項メモの旧仕様(形式ごとに2問生成・押下後全文表示・definition運用)、CLAUDE.mdの検証フロー(旧「ルームタブ→ボット対戦」)、要件定義書 §5.1.2 の定数表(値はコードが正なので出典参照に戻した)。空フォルダ `Views/Home/` を削除

---

## 旧リリース工程(8/11公開計画・8/7に延期決定)

| 日 | TOKIYA-YAMAMOTO(Firebase) | たける(人間作業) | Claude(実装) |
|---|---|---|---|
| **8/04(火)** | **Firebaseコンソール設定**(匿名Auth / RTDB / Firestore を有効化 → `GoogleService-Info.plist` をたけるへ共有)★全体の起点 | アプリ名を決める → App Store Connect で名前の空きを確認しアプリレコード作成 / アイコン着手 | ボットの早押しを表示率ベースに修正 |
| **8/05(水)** | ルール強化 #1(`rooms` を参加者のみ書き込み可に) | **実機2台で早押しの体感遅延を検証** | 対戦画面の演出・アニメーション |
| **8/06(木)** | ルール強化 #2・#3(users の list 禁止 / invites 制限) | 説明文100語のレビュー / アイコン仕上げ | フレンドコード逆引き(`friendCodes/{code}`)のアプリ側対応 |
| **8/07(金)** | 予備(Cloud Functions は v1.5 なので着手しない) | ~~プライバシーポリシー公開~~(8/4完了)/ スクリーンショット撮影 / 審査用メタデータ入力 | ~~問題データを200語へ拡充~~(8/4完了)/ 予備 |
| **8/08(土)** | — | 実機で通し確認(切断・再入室・音・振動)→ **Archive → 申請** | 不具合修正・リリースビルド調整・申請前チェック |
| **8/09(日)** | — | 予備日(リジェクト時は即修正して再申請) | 同左 |
| **8/10(月)** | — | 審査対応 | 待機・修正対応 |
| **8/11(火)** | — | **公開**(App Store Connect のリリース設定は「手動」にして日付を自分で決める) | — |

### 判断ゲート(8/5 終了時点)— **クリア済み**

Firebaseコンソール設定は8/5にTOKIYA-YAMAMOTO氏が完了(Simulatorでの疎通確認まで)。たけるが引き取る必要はなくなった。彼のgit push問題も解消済み(8/5に本人名義のpushを確認)。

**残る受け渡し:`GoogleService-Info.plist` は彼のMacにのみ存在する。** git管理外なのでファイル送付が必要(`FIREBASE_SETUP.md` §3)。これが届くまで、たける側では通信対戦を検証できない。

### v1.0.1へ送る(今回は実装しない)

ルーム自動削除 / 復習リストの表示方針(文字送り型の説明文が長い件)/ `BotBattleSession` のテスト / 問題データ300語化 / 演出の作り込み(1日ぶんに縮小)

---

## 過去の更新(2026年8月8日)

- Codex(たける側): **対戦タブを、起動直後に遊び方を選べるレイアウトへ再設計**。
  - 端末のローカル日付で集計する「連続◯日」「今日◯問」の小さなステータス行を追加し、招待バナーをその直下へ移動
  - 「ひとりで」「オンライン」を左右に並ぶ大きなカードに変更。オンラインはシートからルーム作成・参加を選ぶ構成にした
  - 継続日数は最新の解答日が今日か昨日の場合だけ、重複を除いた解答日を1日ずつ遡って集計。集計処理は`Services/LearningActivitySummary.swift`へ分離
  - 検証:テスト25件パス(集計テスト6件を追加)。Simulatorで起動時の対戦タブ、オンラインの作成・参加導線、CPU対戦10問の完走を確認し、対戦後に「今日0問」から「今日1問」へ増えることを確認

- Codex(たける側): **オンラインロビーに招待リンク共有を追加し、BattleScoreBoardのPreviewを修復**。
  - オンライン対戦の参加コード直下に`ShareLink`を追加。共有文面は「マナビートで対戦しよう!」とルームコードを含み、CPU対戦ではボタンを表示しない
  - App Store URLは`Services/AppLinks.swift`の`AppLinks.appStoreURL`へ集約(未申請中は`nil`でURL行を省く)
  - 検証:Simulatorで共有シートの文面とCPUロビーに共有ボタンが無いことを確認。テスト19件パス

- Claude(たける側): **画面構成を「対戦 / 学習 / マイページ」の3タブへ再編**(起動時は対戦タブ)。学習タブへ一人練習・復習・成績を、マイページへプロフィール・フレンド・設定・お知らせ・作問予告・ポリシーを集約。`HomeView` と `RoomHubView` は削除。UI表記の「ボット」を「CPU」に統一(内部IDは変更なし)。検証:3タブ表示・CPU対戦完走・設定/ポリシー遷移を確認、テスト19件パス

- 要件定義・スコープの確定(たける + Claude):タブ3本 / モードは「ひとりで・オンライン」/ 招待リンク共有 / パスアンドプレイ不採用 / チャット・SNS作らない(スタンプで代替)/ 課金はv1.0なし(将来は問題パック買い切り)/ プロフィール・スタンプ・習熟度=v1.1、作問・目的別カテゴリ=v1.5(中学/高校カテゴリと各★1〜5の基盤は8/8に前倒しで実装)。詳細は要件定義書 §3〜§5・§9

---

## 過去の更新(2026年8月7日)

- Claude(たける側): **設定をホーム右上へ移動し、空いたタブに作問を置いた**。
  - タブバー5番目が「設定」→「**作問**」に。設定はホーム画面右上の歯車から開く(`HomeView` のツールバー)
  - 作問画面(`Views/Creation/QuestionCreateView.swift`)は**次回アップデートで実装予定**の告知。何ができるようになるか(自分で問題を作る/友達と共有/テスト範囲に合わせる)を3項目で見せている
  - 検証:テスト19件パス。Simulatorでタブの入れ替わり・歯車から設定への遷移・作問画面の表示を確認
  - **審査上の注意**:機能しないタブは App Review ガイドライン 2.1(プレースホルダ)に触れる可能性がある。申請前に「実装する」か「タブを外す」かの判断が必要

- Claude(たける側): **回答方式を大幅変更(即答×文字送りの融合)**。早押しボタンを廃止し、**選択肢のタップ自体が「早押し+回答」**になる方式にした(要件 §5.1.2 を書き換え)。
  - 英単語を**1文字ずつ0.8秒間隔で表示**(a → ab → aba → …)。**4択は問題開始時から全員に表示**。分かった瞬間に押す構造
  - 押した瞬間に「選んだ選択肢・回答時刻(サーバー時刻)・その時点の表示文字数」を確定して保持。回答の変更は不可
  - **最初に正解した人がその問題の勝者**(+1)。誤答した人はその問題に再回答できない(−1)が、他の人は続行できる。全員が答え終えたら待たずに発表へ進む
  - 表示間隔は `ProgressiveReveal.characterInterval` の1定数で調整できる
  - **速答型(従来の早押しボタン方式)は残した**。申請日に新方式が仕上がらない場合の逃げ道として、設定で戻せる状態を維持している
  - データを1セット(200問)に戻した。出題形式は見せ方の違いなので `Question.styleRaw` を削除(SwiftDataの移行と再投入をSimulatorで確認済み・200問)
  - 通信側はRTDBに `game/answers/{uid}` を追加し、ホストが**押した時刻の早い順**に採点する方式へ作り替えた(`OnlineBattleSession+Host.judgeAnswers`)
  - **ペース調整**:最初の実装(0.45秒/文字・ボット即答)では10問が1分未満で終わり人間が1問も取れなかったため、0.8秒/文字にし、ボットに「選択肢を読んで選ぶ時間」(強0.8〜1.8秒/中1.5〜3.0秒/弱2.5〜4.5秒)を持たせた
  - 検証:テスト19件パス。Simulatorのボット対戦で**1文字ずつの表示と4択の常時表示**を確認(スクリーンショット)。**人間が勝てるかの決定的な確認は未完**(私の操作往復が1タップ約5秒かかるため、実機でたけるさんが触るのが確実)

- Claude(たける側): **Firebaseのplistを受領して配置し、疎通を確認**。フレンドタブにマイコード(YGMXQL)が表示され、匿名Auth + Firestore の書き込みが通ることをSimulatorで確認。plistは `.gitignore` 済みでコミットされていない

## 過去の更新(2026年8月5日)

- Claude(たける側): **対戦画面の演出・アニメーションを実装**(次のタスク6)。
  - **得点の変化**:数字が入れ替わるトランジション(`contentTransition(.numericText)`)。正解した人のカードを緑の枠と背景で強調
  - **正解発表**:緑のカード+チェックアイコンで「誰が取ったか」を明示。出現時にスケール+フェード。時間切れは灰色のまま区別
  - **残り時間**:数値(残りN秒)を追加し、5秒を切ると赤く点滅。早押しの受付が終わっている間は時間を出さない
  - **早押しボタン**:待機中はゆっくり脈動+赤いグロー、押下時は縮んで即座に手応えを返す(通信の返事を待たずに反応する)
  - **問題の切り替え**:フェードで入れ替える
  - View を分割(`BattleScoreBoard` / `BuzzButton` / `BattleRevealCard` / `BattleProgressHeader`)。動きの時間は `BattleAnimation` に集約し、画面ごとにバラつかないようにした
  - 検証:テスト21件パス。Simulatorのボット対戦で**発表カード・得点者の緑ハイライト・残り秒数・脈動するボタン・文字送り**の表示をスクリーンショットで確認

- 人間: GitHubのPrivateリポジトリをMacへcloneし、`xcodegen generate` 実行後にSimulatorでアプリがクラッシュせず起動することを確認
- 人間: **Firebase基本セットアップを完了**。Firebaseプロジェクト `hayaosiapp` を作成し、Bundle ID `com.n.HayaosiApp` でiOSアプリを登録。Google Analyticsは無効、Sparkプランを使用。匿名Authentication、asia-southeast1のRealtime Database、Cloud Firestoreの`(default)`データベースを有効化し、`FIREBASE_SETUP.md` 記載の暫定セキュリティルールを公開
- 人間: Realtime Database作成後の `GoogleService-Info.plist` を `HayaosiApp/Resources/` に配置。BUNDLE_IDが `com.n.HayaosiApp` であり、DATABASE_URLとGOOGLE_APP_IDが存在すること、およびplistがGit管理外であることを確認
- 人間: **SimulatorでFirebase疎通を確認**。Firebase未設定案内の消失、6桁のマイコード表示、匿名ユーザー作成、Firestoreの `users/{uid}` ドキュメント作成、Realtime Databaseの `rooms/{4桁コード}` 作成を確認
- **実機2台でのルーム参加・通信対戦は未実施**。早押しの体感遅延、同時押し判定、誤答後の回答権移行、切断・再入室、ホスト切断時の挙動は未確認
- App Store公開用の本格セキュリティルールは未対応
- Claude(たける側): 上記ブランチ `codex/integrate-origin-main` を `main` へ取り込み(fast-forward・コンフリクトなし)。**彼のpushが通ることを確認**(8/5 17:45、本人名義)。plistはリポジトリに含まれていないことも確認済み

## 過去の更新(2026年8月4日)

- Claude(たける側): **「plistがコミットできない」で詰まる構造を解消**(こちら側の不備の修正)。
  - 原因:`GoogleService-Info.plist` を `.gitignore` に入れているため `git add` が無言で無視される(赤いエラーが出ない)。それを `FIREBASE_SETUP.md` の括弧書き1行にしか書いていなかったため、**受け取り側からは「pushできない」に見えていた**
  - 対応:FIREBASE_SETUP.md §3 を「受け渡しと配置」として書き直し、gitでは渡せない理由と手順を警告として明示。`.gitignore`・README・CLAUDE.md にも同じ注意を追記。READMEのエラー切り分け表に「エラーが出ず変更なしで終わる場合」と「`! [rejected] (fetch first)`」の行を追加
  - なお**強制push(force push)は一度も行っていない**(reflogで確認済み。履歴は一直線)。他のメンバーのクローンが壊れる操作はしていない

- Claude(たける側): **英単語データを200語に拡充**(工程8/7分を前倒し)。動詞80・名詞70・形容詞50で、追加分も「単語・品詞・意味・説明文」の4項目をそろえた。出題数は**速答型200問・文字送り型200問(計400問)**。
  - 品詞ごとに意味・説明文の重複がないこと、誤答の巡回ストライド(11)が各品詞の語数と互いに素であること、**全400問で選択肢が4つそろうこと**をスクリプトで検証済み
  - `dataVersion` を3に上げたので既存端末は次回起動時に再投入される。旧データからの再投入をSimulatorで確認済み(200/200件)
  - **追加した100語の日本語レビューはたける担当**(既存100語と同じく)

- Claude(たける側): **アプリ名を「マナビート」に決定し反映**。表示名(`INFOPLIST_KEY_CFBundleDisplayName`)とホーム画面のみ変更し、Bundle ID・ターゲット名・リポジトリ名は `HayaosiApp` のまま。プライバシーポリシーのページも更新済み。Simulatorでホーム画面・アプリ名の表示を確認

- Claude(たける側): **ボットの早押しタイミングを問題文の表示量基準に変更**(Day1分)。
  - 文字送り型では「説明文がどこまで表示されたら押すか」で決めるようにした(強=35〜70% / 中=50〜85% / 弱=70〜100%)。従来は表示量に関係なく1.2〜9秒で押していたため、人間が読む前に取られていた
  - 速答型と、文字送り型で誰かが押した後(全文表示になっている状態)は従来どおり秒基準。判定は `OnlineBattleView` の全文表示条件と同じロジックに揃えた
  - 表示割合→時間の変換は `ProgressiveReveal.time(forVisibleFraction:timeLimit:)` に置き、表示量の計算と出典を1つにした
  - 検証:テスト21件パス(新規3件)。Simulatorのボット対戦(文字送り型・ボット強+中)で**人間が2体より先に押せることを実際に確認**。その後10秒放置して時間切れ→−1点→お手つきロック→全文表示の流れも確認済み

- Claude(たける側): **ドキュメントの用語と前提を整理**(コード変更なし)。
  - 「出題形式(速答型/文字送り型=`QuizStyle`)」と「回答形式(4択/入力式=`QuestionType`)」を別の軸として要件定義書で言い分けた(以前は問題形式・出題形式・回答形式の3語が混在していた)
  - `FIREBASE_SETUP.md` のデータ構造に `settings.style` を追記(実装に追従できていなかった)。要件定義書 §8.1 の Question・§9 の一人練習の設定にも出題形式を追記
  - **`xcode-select` がXcode本体を指している必要がある**ことをREADMEの前提とCLAUDE.mdに明記(Simulator操作ができずハマった)。テストコマンドの機種名は各自の環境で確認する旨も追記
  - 動作確認ができない場合は**「未検証」を明記して報告する**ルールをCLAUDE.mdに追加(できない前提が書かれていないと黙って省略される)
  - 作業中宣言のルールを実態に合わせた:コミット判断は人間が持つため、**pushされないまま作業が続く場合は宣言を残す**(以前の「必ず先にpushする」は守れない場面がある)

## 過去の更新(2026年8月3日)

- Claude(たける側): **出題形式を2種類にして切り替えられるようにした(文字送り型を標準に)**。要件 §5.1.2。
  - **文字送り型(標準)**:意味・説明文を少しずつ表示 → 単語を4択。早く押すほど手がかりが少ないので押すか待つかの駆け引きが生まれる
  - **速答型**:従来どおり単語 → 意味を4択。設定で選べる。どちらを主にするかはリリース後の反応で見直す前提
  - `QuizStyle`(`Models/QuizTypes.swift`)を追加し、`Question` に `styleRaw` を追加。**1単語から形式ごとに2問を生成**(`en_XXXX`=速答型 / `pw_XXXX`=文字送り型)。IDが別なので復習リスト・履歴も形式ごとに独立して積まれる
  - `english_words.json` に **`definition`(説明文)を100語ぶん追記**(平均22字)。従来の `meaning` は平均5.6字で、25語は3字以下(「証拠」など)だったため文字送りが機能しなかった。`definition` が無い語は `meaning` にフォールバックする。**日本語の言い回しはたけるのレビュー希望**
  - 表示量の計算は `Services/Battle/ProgressiveReveal.swift`(制限時間の6割で全文。経過時間だけで決まるので全端末で同じ見え方)。文字送りは**誰も押していない間だけ進み、誰かが押したら全文表示**にした(回答者が読めないと答えられず、誤答で回答権が移った人も読み直す必要があるため)
  - 設定UIは `Views/Components/QuizStylePicker.swift` に共通化し、ルーム作成・ボット対戦・一人練習の3画面に配置。ロビーの設定一覧にも「出題形式」を表示。対戦の出題形式はRTDBの `settings.style` で全員に同期
  - `QuestionSeeder.dataVersion` を 2 に上げたので、既存端末は次回起動時に問題データが再投入される(履歴・復習リストはIDで別管理なので消えない)
  - 検証:Simulatorビルド + テスト18件パス(`ProgressiveReveal` の8件を新規追加)。Simulatorに再インストールして起動し、SwiftDataに速答型100問・文字送り型100問が入り、旧スキーマからのSwiftData移行も問題なく通ることを確認
  - **Simulatorでの目視確認済み(2026年8月4日)**:ボット対戦(文字送り型・制限時間30秒)で、問題文が「知識や技術を、時間▍」のように途中まで表示され、その状態で早押しボタンが押せることを確認。ボットが押した後は全文表示に切り替わる挙動も確認
  - **目視で見つかった問題**:ボットが問題文の表示量に関係なく1〜7秒で押すため、**文字送り型では人間が押す前にほぼ取られる**(10問で自分0pt・ボット−2/−4ptという、ボットが早押しして誤答を繰り返す展開になった)。ボットの早押しタイミングの見直しが必要(下記タスク4)

- Claude(たける側): **ドキュメントの棚卸しと開発基盤の整備**(コード仕様の変更はなし)。
  - **要件定義書**:実装済みの早押しルール(選択肢は押した本人にのみ表示・お手つきロック・回答10秒・発表3秒・全員誤答で仕切り直し)を §5.1.2 に明記。フレンド機能(§5.6)・ボット対戦(§5.7)をv1.0スコープとして追加。Firestore/RTDBの役割分担を実装に合わせて修正。未決定事項を棚卸し(人数8人・レイテンシ500msは暫定確定、入力式はv1.5へ延期、Bundle IDは改名しても変えない)。冒頭に「この文書は大枠まで。実装詳細の正はSTATUS.md」と位置づけを追記
  - **未決定として新規に立てた項目**:**早押しの駆け引き方式**(案A速答型/案B定義文の逐次表示/案C配点減衰型)。現状は単語1語を出すだけで押すタイミングの駆け引きが無いという指摘への対応。要件定義書 §5.1.2 と §10 #7 に記載。Claudeの推奨は案B(既存100語に定義文を足すだけで済み、得点ルールに波及しない)
  - **CLAUDE.md**:ボット対戦での検証フロー・担当分担・テスト方針・ブランチとコミット規約・`BattleRules`の置き場所・効果音の差し替え方を追記。フェーズ記述はSTATUS.mdへ一本化(二重管理をやめた)
  - **AGENTS.md**:文書の優先順位(スコープの正=要件定義書 / 実装詳細の正=STATUS.md)を明記。作業中宣言は**先にpushしないと相手に見えない**ことをルール化(CLAUDE.mdにも反映)
  - **署名設定を分離**:`project.yml` に固定されていた `DEVELOPMENT_TEAM`(たけるのチームID)を `Config/local.xcconfig`(git管理外)へ移動。相方がcloneして実機ビルドすると必ず署名エラーになる状態だった。`Config/local.xcconfig.sample` をコピーして各自のIDを書く運用。**このファイルが無くてもSimulatorビルドは通ることを確認済み**
  - **`BattleRules` を `Services/Battle/BattleRules.swift` へ移動**(旧:`Services/Online/OnlineBattleSession.swift` 内)。Firebaseをimportするファイルにオフライン用の定数が同居していたため
  - **テストターゲットを追加**(`Tests/` + `HayaosiAppTests`)。`QuizSession` のテスト10件(正誤判定・時間切れ・問題送り・終了判定)を作成し全パス。テスト方針はCLAUDE.mdに記載
  - **FIREBASE_SETUP.md**:暫定ルールがリリースブロッカーであることを明示し、リリース前に必要な強化を §4-1 として表に整理。**フレンドコード検索を `users` コレクションのクエリで実装しているため全ユーザーのニックネーム・コードが列挙できる**問題と、`friendCodes/{code}` 逆引き方式への変更案を追記。DBはロックモードで作る手順に修正
  - `MARKETING_VERSION` を 0.5.0 → 0.9.0(実態はv1.0クライアント完成・未検証)
  - Simulatorビルド成功・テスト10件パスを確認

## 過去の更新(2026年7月13日)

- Claude(たける側): **効果音・ハプティクスを追加**。
  - 効果音6種を正弦波合成で自前生成(著作権フリー、`Resources/Sounds/se_*.wav`。同名ファイル差し替えで変更可):早押し「ピコッ」/正解「ピンポン」/不正解「ブブー」/時間切れ(下降2音)/出題「ポッ」/リザルトファンファーレ。生成スクリプトはコミットせず(必要なら再生成可)
  - `SoundPlayer`(`AVAudioPlayer`・起動時プリロード・`.ambient`=マナーモード時は無音・音楽と共存)と `Haptics`(`UIImpactFeedbackGenerator`)を新設
  - 配線:対戦画面(出題/早押し=回答権確定時/誤答時の回答権移行/正解・時間切れ発表/リザルト+早押しボタンheavy振動・選択肢light振動)、一人練習(出題/正誤/時間切れ/リザルト+選択肢振動)
  - 設定に「サウンド」セクション(効果音・振動のON/OFFトグル)を追加。デフォルトON
  - Simulatorビルド成功・バンドルに6音源が含まれることを確認。**実機での音の聞こえ方の確認はたける担当**(ボット対戦で試せる)
- Claude(たける側): **ボット対戦(ローカル)を追加** — Firebase未設定でも対戦画面のUI開発・練習が可能に。
  - 共通プロトコル `BattleSession`(`Services/Battle/`)を新設し、ロビー/対戦/リザルト画面はプロトコル越しに描画するよう変更(`any BattleSession`)。オンライン対戦(`OnlineBattleSession`)とボット対戦(`BotBattleSession`)が同じ画面を共有
  - `BotBattleSession`:オンライン版と同じルール(先着1名に回答権・正解+1/不正解−1・誤答で仕切り直し・回答10秒・発表3秒)をローカルで再現。ボットは強/中/弱の3プロファイル(早押し速度・正答率が異なる)で1〜3体
  - 入口:ルームタブに「ボット対戦」カード(オレンジ・cpuアイコン)→ 設定(問題数/制限時間/ボット数)→ ロビー → 対戦 → リザルト(再戦可)
  - ボット対戦の結果は**一人練習扱い**(`AnswerRecord` mode: practice)で復習リストにも反映
  - ロビーはボット時に参加コード・フレンド招待セクションを非表示。ボットはPC アイコンで表示
  - Simulatorで実際に1試合完走を確認(ボットの早押し→正解発表→スコア加算→リザルト順位表示)
  - **UI改善はこれから**:対戦画面の見た目を磨く際は `Views/Battle/` を編集し、ボット対戦で確認する運用

- Claude(たける側): **Firebase未設定でも新UIを表示するプレビューモードを追加**。フレンド・ルームタブは未設定時、セットアップ案内画面の代わりにサンプルデータ(ときや・はなこ/招待バナー)で実UIを表示し、上部にオレンジの「サンプル表示中」バナーを出す。ネットワークが必要な操作(追加・削除・招待参加)はタップ時にアラート案内。マイコードは誤共有防止のため`SAMPLE`表示。不要になった`OnlineSetupRequiredView`は削除。Simulatorでフレンド・ルーム両タブの表示確認済み

## 過去の更新(2026年7月12日・夕方〜夜)

- Claude(たける側): **フレンド・ルームタブのUI刷新**(UI構想3案から選定:フレンド=プロフィールカード型、ルーム=2大カード+招待バナー型)。
  - フレンドタブ:自分の「会員証」風プロフィールカード(アバター・ニックネーム・コード・コピー/シェアボタン)+フレンドのアバターグリッド(色はuidから決定的に選択)+破線「追加」タイル→コード入力シート。削除は長押しコンテキストメニュー
  - ルームタブ:フレンドからの招待をオレンジのバナーとして最上部に表示(参加/却下ボタン付き)。作成・参加カードは従来どおり
  - 新規ファイル:`Views/Components/AvatarCircle.swift` / `Views/Friend/FriendProfileCard.swift` / `Views/Friend/AddFriendSheet.swift`。Simulatorビルド成功。※Firebase未設定のため実表示確認はXcodeプレビュー(`#Preview`)または設定後に実施
- Claude: **GitHub化**。`saikyo-app-team` 組織にPrivateリポジトリ [HayaosiApp](https://github.com/saikyo-app-team/HayaosiApp) を作成しinitial commitをpush。`.xcodeproj`は引き続きgit管理外(XcodeGenで生成)のため、clone後の手順を`README.md`に追記。もう一人の開発者もこのリポジトリをcloneして参加できる状態
- Claude: **実機ビルドエラーを修正**。原因は署名チーム未設定(「Signing for "HayaosiApp" requires a development team」)。`project.yml` に `DEVELOPMENT_TEAM: LL98RL72H4`(Buffitoと同じチーム)を追加して再生成し、実機向けビルド(自動署名)成功を確認
- Claude: **タブバー化・フレンド機能・通信対戦を実装**。
  - **タブ構成**:`RootTabView` を新設し、ホーム(一人練習+成績サマリ)/ルーム/フレンド/復習/設定 の5タブに再編。旧スタブ(`LobbyView`・モック`BattleView`)は削除
  - **Firebase導入**:SPMで firebase-ios-sdk 12+(Auth / Database / Firestore)を追加。`GoogleService-Info.plist` が無くてもビルド・オフライン機能は動作し、オンライン系タブにはセットアップ案内を表示(`OnlineService.configureIfPossible`)。**人間作業の手順は `FIREBASE_SETUP.md`**
  - **フレンド機能**(`AuthService` / `FriendService`):匿名サインイン+6桁フレンドコード自動発行(users/{uid})、コード入力でフレンド追加(片方向)、一覧・削除、ロビーからのルーム招待送信と、ルームタブでの招待受信→参加
  - **通信対戦**(`OnlineBattleSession` + `+Host.swift` / `RoomState`):4桁コードのルーム作成・入室(2〜8人)、ロビー同期、早押しはRTDBトランザクションで先着1名を原子的に確定+押下順キュー保持、正解+1/不正解−1、誤答時は次の押下者へ回答権移行(全員誤答なら制限時間を仕切り直し)、出題タイムアウト、回答10秒制限、正解発表3秒→次問、リザルト(同点同順位)、ホストの再戦、切断時のonDisconnect処理(ホスト切断=ルーム解散)。進行の権威はホスト端末
  - **対戦結果の学習履歴反映**:自分が関与した問題の正誤を `AnswerRecord`(mode: battle)と復習リストへ保存
  - Simulatorビルド成功・起動確認済み(タブ表示・ホーム画面・問題データ100問)。**通信対戦の実動作は未検証(Firebase未設定のため)**

## 過去の更新(2026年7月12日・午前・初期構築)

- Claude: プロジェクト初期構築。①開発ドキュメント(CLAUDE.md / AGENTS.md / STATUS.md)を作成 ②XcodeGen構成(`project.yml`、iOS 17+・iPhone専用・SwiftData)③v0.5の骨組みを実装:SwiftDataモデル(`Question` / `AnswerRecord` / `ReviewItem`)、出題エンジン`QuizSession`(練習・対戦共通。制限時間・正誤判定・フェーズ管理)、英単語100問(`Resources/english_words.json`。初回起動時に自動投入、誤答選択肢は同品詞の他単語から自動生成)、画面一式(ホーム/練習設定/出題/リザルト/復習リスト/設定)④v1.0用スタブ画面(ルーム作成・ルーム参加・待機ロビー・対戦UIモック。通信なし、「v1.0で実装予定」を画面に明記)。Simulatorビルド成功を確認済み
